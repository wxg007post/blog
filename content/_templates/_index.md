---
title: "写作模板（本页不会被发布）"
build:
  render: never
  list: never
cascade:
  build:
    render: never
    list: never
---

这个目录放**写作模板**，供 Obsidian 的「模板」插件使用，**不会出现在网站上**。

- `_文章模板.md`：新建文章时用它（Obsidian 设置 → 模板 → 模板文件夹选 `_templates`）。
- 目录名以 `_` 开头是为了让它在文件列表里排在前面，便于识别。
