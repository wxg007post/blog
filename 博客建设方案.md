# 个人博客建设方案

> 目标：把一个「折腾记录 + 经验总结 + 教程攻略」的个人博客，稳定地跑在 GitHub Pages 上，绑定自己的域名 `blog.wxgg.eu.cc`，写作体验对非程序员足够友好。
> 方案版本：v1.10（2026-09-12）｜状态：**方案定稿 + 阶段 0 已完成**（Hugo 0.165.0 / Git 2.53.0 / GitHub Desktop / Obsidian 全部就绪并逐项验证通过，证据见《操作手册》），17 条决策锁定，**等待指令进入阶段 1**

---

## 5 分钟版（只看这一段也能知道全貌）

| 问题 | 答案 |
|---|---|
| 博客是怎么做出来的？ | 你在电脑上写 Markdown → 推送到 GitHub → GitHub 自动编译成网页 → 免费托管在 GitHub Pages → 用 Cloudflare 挂上你的域名 `blog.wxgg.eu.cc` |
| 我要学编程吗？ | **不需要**。日常只做两件事：**写 .md 文件**、**用 GitHub Desktop 点两下发布** |
| 花多少钱？ | **0 元/年**（域名已有；Pages / Actions / Cloudflare 全免费，见第 8 章） |
| 用什么技术？ | **Hugo 0.165.0 extended**（版本必须锁死，见 3.5）+ **Blowfish v3** 主题 |
| 国内访问快吗？ | GitHub 裸地址不稳，所以前面套 Cloudflare 加速（见 4.1） |
| 要备案吗？ | **不需要**（服务器在境外，见 4.3） |
| 必须提前避开的坑 | ① Hugo 版本**不能高于 0.165.0**（超了直接构建失败）② `baseURL` 必须和域名一致，否则**样式全丢**（见 4.6）③ **公开仓库 = 每个字都公开**，敏感信息绝不能写进去（第 7 章） |
| 搜索支持中文吗？ | 支持，但**默认不好用**，需要按 3.6 改，且**必须实测**（见 15.2） |
| 多久能看到效果？ | 阶段 0→3 一个下午就能上线，剩下是持续写内容（第 6 章） |
| 怎么算"做完了"？ | 按**第 15 章验收清单**逐项打勾 |

**怎么读这份文档（按需挑）**：

- **只想看结论、不想看原理** → 上面这张表 + 第 6 章（路线图）+ 第 15 章（验收清单），其余章节**扫一眼标题即可**。
- **想知道每个决定为什么这么定** → 第 3 章，每条都标了核实依据。
- **担心安全和隐私** → 直接看**第 7 章**，那是全篇最要紧的一章。
- **硬核技术细节**（Fuse.js 参数、配置项、CI 工作流）→ 集中在 3.4 / 3.6 / 第 14 章，**你不必看懂**，落地时由我来配。
- **日常写作时** → 你只需要那份**独立的《操作手册》**（只讲"点哪里"，1~2 页），**不必再打开这份方案**。这份文档保持**单文件**是有意的：它是这个项目的"知识库"，拆成多份反而容易出现前后对不上；真正需要轻量的是手册，而不是它。

---

## 决策记录（实时更新）

| #   | 决策项            | 结论                                                                                    | 状态   |
| --- | -------------- | ------------------------------------------------------------------------------------- | ---- |
| 1   | 写作方式           | 电脑上用 Markdown 编辑器写，一键发布到线上                                                            | ✅ 已定 |
| 2   | 域名             | `blog.wxgg.eu.cc`，NS 在 Cloudflare；原指向的服务已清理，可放心接入                                     | ✅ 已定 |
| 3   | 站点框架           | **Hugo**（extended 版，**版本锁定**见 3.5）                                                    | ✅ 已定 |
| 4   | 主题             | **Blowfish v3**（中文文档齐全，支持站内搜索）                                                        | ✅ 已定 |
| 5   | 仓库策略           | **公开仓库**（免费账号的 Pages 要求站点仓库公开）                                                        | ✅ 已定 |
| 6   | 评论与统计的上线时机     | **站内搜索本次上线**；**评论与统计都不做**（统计将来可能再加，评论不考虑）                                             | ✅ 已定 |
| 7   | 本地 Git 方案      | GitHub Desktop（图形化，适合非程序员） ／ 命令行 Git                                                  | ✅ 已定 |
| 8   | GitHub 用户名（事实项，非决策）  | `wxg007post`；CNAME 目标为 `wxg007post.github.io`                                                       | ✅ 已定 |
| 9   | 站内搜索           | **本次上线**，且必须支持中文搜索（实现与调优见 3.6）                                                        | ✅ 已定 |
| 10  | 评论             | **不做**（Blowfish 本就不内置评论服务，正好省事）                                                       | ✅ 已定 |
| 11  | 访问统计           | **本次不做**；将来若需要再上 Umami（Blowfish 原生支持）                                                 | ✅ 已定 |
| 12  | 图床             | **本次不做**；将来用 Cloudflare R2 承载截图                                                       | ✅ 已定 |
| 13  | 仓库名与站点类型       | **项目站点**：仓库名 `blog`，绑定域名后地址为根目录                                                       | ✅ 已定 |
| 14  | 中文显示（Hugo CJK） | **开启** `hasCJKLanguage = true`（不开的话中文文章会显示「1 词 · 1 分钟阅读」，见 3.4.6）                     | ✅ 已定 |
| 15  | 代码复制按钮         | **开启** `enableCodeCopy = true`（⚠️ `ShowCodeCopyButtons` 是 PaperMod 的参数，在 Blowfish 无效） | ✅ 已定 |
| 16  | 内容分类体系 | **分类 2 个**（折腾记录；**攻略＝更宽的类**：游戏攻略 + 选购选型 + 方案对比）+ **标签初始 10 个**（`NAS`、`飞牛`、`Docker`、`OpenWrt`、`ImmortalWrt`、`OpenClash`、`Tailscale`、`软路由`、`AI`、`工具`），可随时扩充（判定规则见 5.3） | ✅ 已定 |
| 17  | 写作编辑器 | **Obsidian**（免费）—— MarkText 实测不合格（插图写成**绝对路径**、图片被丢到 `D:\` 根目录且用 64 位哈希文件名）；Typora（收费）作为备选 | ✅ 已定 |

> 想改哪一条，直接改表格里的结论，或在附近加一行 `> ❓ 疑问：...`；改完告诉我，我复核。

---

## 0. 一句话结论

**内容全部是纯 Markdown 文本 → 存进 GitHub 仓库 → GitHub 的服务器自动帮我"编译"成网页 → 发布到 GitHub Pages → 用 Cloudflare 套在自己的域名上加速。**

你日常只需要做三件事：**写 .md 文件 → 点一下发布 → 2 分钟后网页自动更新**。本地不需要装任何重型环境（连 Node 都不是必需的），也不需要买服务器。

推荐主线（**已定稿为路线 A**）：

| 路线           | 框架       | 主题                | 适合                             |
| ------------ | -------- | ----------------- | ------------------------------ |
| **A ✅ 已选定**  | **Hugo** | **Blowfish**      | 省心、构建快、几乎不会坏、主题好看且中文文档全        |
| B（备选，仅作日后参考） | Hexo     | Butterfly 或 Fluid | 中文教程最多，遇到问题最好搜；但依赖 npm 插件，偶尔要修 |

---

## 1. 现状盘点（已确认）

| 项目         | 现状                                                                                       | 影响                                                             |
| ---------- | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 工作区        | `D:\DSH\my_blog`（空）                                                                      | 从零开始，正好干净                                                      |
| Node / npm | v24.21.0 / 11.19.0 已装                                                                    | 本方案用 Hugo，Node 不是必需的（留着不影响）                                    |
| Git        | **未安装**                                                                                  | 由 GitHub Desktop 自带，不必单独装命令行 Git                               |
| Hugo       | ✅ 已安装 0.165.0 extended                                                                                      | 已解压到 `%LOCALAPPDATA%\Programs\hugo` 并加入用户 PATH；本机 winget 不可用，走手动解压（见 3.4.1 实测记录）                                     |
| 域名         | `blog.wxgg.eu.cc`，NS 在 Cloudflare（`margot.ns.cloudflare.com` / `odin.ns.cloudflare.com`） | 加速和 HTTPS 有天然优势                                                |
| 本机 DNS 观察  | 解析到 `198.18.6.67`                                                                        | 这是 **OpenClash 的 fake-ip 假地址**，不是真实解析结果；真实记录请在 Cloudflare 后台核对 |
| 关键风险       | 博客是**全网公开**的                                                                             | 你的内容涉及内网/密钥，必须脱敏（见第 7 章）                                       |

**域名状态（已确认）**：`blog.wxgg.eu.cc` NS 在 Cloudflare，原解析已清理，可直接接入。

---

## 2. 整体架构

```
【写作端】                       【云端】                              【访问端】
Obsidian / Typora / VSCode
  写 .md 文件
      │
      ▼
 GitHub Desktop / publish.cmd
  「提交 + 推送」一次性完成
      │
      ▼
   GitHub 仓库  ──► GitHub Actions（自动构建，约 30~90 秒）
  （你的源码）              │
                            ▼
                    静态 HTML 文件
                            │
                            ▼
                   GitHub Pages（免费托管）
                            │
                            ▼
              Cloudflare（DNS + CDN + 缓存 + HTTPS）
                            │
                            ▼
                     https://blog.wxgg.eu.cc
```

要点：

- **源码即内容**：仓库里只有 Markdown 和图片，任何编辑器都能写，将来想换框架/换托管平台，内容一个字都不用改。
- **构建在云端**：不需要为了发文章在自己电脑上装一堆编译环境，换台电脑（甚至用 iPad）也能发。
- **CDN 在 Cloudflare**：国内访问 GitHub Pages 的裸地址通常不稳定，套 Cloudflare 后体验明显改善，而且这个域名本来就在 CF。

---

## 3. 技术选型

### 3.1 静态站点生成器对比

| 维度      | Hugo               | Hexo              | Astro           |
| ------- | ------------------ | ----------------- | --------------- |
| 本地依赖    | **一个 exe，零依赖**     | Node + npm + 一堆插件 | Node + 一堆 npm 包 |
| 构建速度    | 极快（毫秒~秒级）          | 中等（几十秒）           | 中等              |
| 主题数量/质量 | 多，且维护规范            | 多，中文主题最丰富         | 少但都很好看          |
| 中文教程    | 多                  | **最多**（国内主流）      | 少               |
| 出问题概率   | 低（模板语法陌生，但日常几乎不用碰） | 中（插件/Node 版本偶发冲突） | 中高（迭代快，教程易过期）   |
| 换主题成本   | 低                  | 低                 | 低               |
| 非程序员友好度 | ★★★★☆              | ★★★★☆             | ★★★☆☆           |

**结论（已定稿）：选 Hugo + Blowfish。** Astro 不作第一站（好看但折腾成本对非程序员偏高）；Hexo 仅作为日后备选记录在案，本方案后续不再涉及。

### 3.2 主题候选（**已选定 Blowfish**，下表留作日后换主题的备查）

| 主题            | 框架    | 演示站                                                          | 仓库                                                | 风格印象                       | 深色/搜索/分类标签/评论        |
| ------------- | ----- | ------------------------------------------------------------ | ------------------------------------------------- | -------------------------- | -------------------- |
| **Blowfish**  | Hugo  | https://blowfish.page/                                       | https://github.com/nunocoracao/blowfish           | 卡片大图首页，圆角，配色方案多，组件丰富       | 全支持（含中文文档 `/zh-cn/`） |
| **FixIt**     | Hugo  | https://fixit.lruihao.cn/ ｜演示 https://demo.fixit.lruihao.cn/ | https://github.com/hugo-fixit/FixIt               | 双栏博客风，侧栏信息密集，功能型动效多，作者是中国人 | 全支持，中文文档最全           |
| **Stack**     | Hugo  | https://demo.stack.jimmycai.com/                             | https://github.com/CaiJimmy/hugo-theme-stack      | 卡片 + 圆角阴影，右侧栏小部件丰富         | 全支持                  |
| PaperMod      | Hugo  | https://adityatelange.github.io/hugo-PaperMod/               | https://github.com/adityatelange/hugo-PaperMod    | 极简纯文字，留白多，加载最快             | 全支持（功能最少）            |
| **Butterfly** | Hexo  | https://butterfly.js.org/                                    | https://github.com/jerryc127/hexo-theme-butterfly | 卡片鲜艳，封面大图 + 侧栏挂件，动效华丽      | 全支持，国内最流行            |
| **Fluid**     | Hexo  | https://hexo.fluid-dev.com/                                  | https://github.com/fluid-dev/hexo-theme-fluid     | Material 风格，圆角卡片，清爽克制      | 全支持                  |
| Stellar       | Hexo  | https://xaoxuu.com/wiki/stellar/                             | https://github.com/xaoxuu/hexo-theme-stellar      | 知识库/文档向，信息密度高              | 评论需外接插件              |
| Fuwari        | Astro | https://fuwari.vercel.app/                                   | https://github.com/saicaca/fuwari                 | 卡片 + 渐变模糊背景，动效顺滑           | 无内置评论，官方文档英文         |

> 以上链接均已核实有效（演示站是否可直接访问会受你本机网络/代理影响，打不开就先挂代理看）。

**已定稿（2026-09-12）**：**Blowfish（Hugo）**。理由：首页有 7 种布局可选（`page / profile / hero / card / background / landing / custom`），深色模式、分类、标签、Fuse.js 站内搜索、Umami 统计都是原生支持，中文文档齐全。评论按你的决定**不做**，所以这项也不用做了（Blowfish 本就不内置评论服务，正好省事）。

### 3.3 评论 / 统计 / 搜索 / 图床

| 能力        | 推荐                                                         | 说明                                                                                                                                                        |
| --------- | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 站内搜索      | 主题自带（Fuse.js / Pagefind / 本地索引）                            | 静态站就能搜，**不需要服务器**                                                                                                                                         |
| 评论        | **Twikoo**（Vercel 免费部署，或 Docker 跑在你 NAS 上）                 | 访客无需 GitHub 账号，国内可用；备选 Waline（Docker 自建，同样合适）。⚠️ Blowfish **不内置任何评论服务**，需要我自建 `layouts/partials/comments.html`（官方预留了这个扩展位）                                |
| 评论（零成本备选） | Giscus（基于 GitHub Discussions）                              | 运维为零，但要求访客登录 GitHub，国内读者不友好，不建议做主力                                                                                                                        |
| 访问统计      | **Umami**（Docker 自建在 NAS）                                  | Blowfish **原生支持** Umami：`params.toml` 里写 `[umamiAnalytics] websiteid = "..."`；数据自己攥着，和你 NAS 生态很搭。Cloudflare Web Analytics 无原生支持，需走 `extend-head.html` 扩展位 |
| 不推荐       | Google Analytics                                           | 国内加载慢，影响首屏                                                                                                                                                |
| 图床        | 小图直接进仓库；截图多时用 **Cloudflare R2** 或 NAS 上的图床（兰空图床 / PicHome） | GitHub 仓库有 1GB 站点上限，图片是最大杀手                                                                                                                               |

**已按你的决定定稿**：站内搜索**本次上线**；评论与访问统计**不做**；图床本次**不做**（将来用 Cloudflare R2）。

> ❓ 疑问：本次上线要站内搜索功能，
> 
> 评论和访问统计不上线，
> 
> 图床考虑用cloudflare r2，后续再增加，本次暂不上线
> 
> ✅ **已全部采纳（2026-09-12）**：站内搜索本次上线；评论与统计不配（第 6 章阶段 5、第 8 章成本表已同步改掉）；图床延后到 Cloudflare R2。中文搜索的可行性我单独核实过，结论写在 **3.6**。

### 3.4 Blowfish 落地细节（选型已定稿）

> 以下参数取自 Blowfish 仓库一手文档核实（2026-09-12 时点），不是我的记忆。

#### 3.4.1 版本与依赖要求

| 项目            | 要求                                                      | 说明                                  |
| ------------- | ------------------------------------------------------- | ----------------------------------- |
| Hugo          | **锁定 0.165.0**（extended 版；主题声明区间 0.162.0–0.165.0，见 3.5） | **不要装最新版**：高于 0.165.0 会被主题的版本校验直接拒绝 |
| 是否必须 extended | **必须 extended**（主题配置里声明了 `extended = true`）             | 主题的 CSS 虽已预编译，但版本校验硬性要求 extended    |
| Node / npm    | **不需要**                                                 | Tailwind CSS 4 的产物已内置，不需要你本地跑前端工具链  |
| Go            | 仅「Hugo Modules」安装方式需要                                   | 我们不采用这条路                            |

```powershell
# 本机安装（Windows，锁 0.165.0）
# 【最省事】方式一：winget（版本号写纯数字，不带 v）——⚠️ 本机实测 winget 直接报错不可用，见下方记录
winget install --id Hugo.Hugo.Extended --version 0.165.0 --exact
# 【等效】方式二：scoop
scoop install hugo-extended@0.165.0
# 【最可靠 · 兜底】方式三：手动解压——不依赖任何包管理器源的状态
# 下载 https://github.com/gohugoio/hugo/releases/download/v0.165.0/hugo_extended_0.165.0_windows-amd64.zip
# 解压出 hugo.exe 放到固定目录（本机实际用的：%LOCALAPPDATA%\Programs\hugo）并加入 PATH
```

验证：`hugo version` 应输出形如 `hugo v0.165.0+extended windows/amd64`——**必须带 `+extended`**（没有 extended 字样说明装的不是扩展版）。
手动下载的 SHA256 校验值：`C0259EB25D11B7D1EE7CD2F86DA84F17323A36A90DEE0AC0F7A93B635EC837F9`

> **若方式一/二报「找不到该版本」，直接用方式三。** `Hugo.Hugo.Extended` 这个 winget id 我已在 winget-pkgs 仓库里核对存在（0.165.0 的 manifest 在册），`scoop install 应用@版本` 也是 Scoop 官方支持的写法；但**包管理器源的状态会变**（版本下架、源同步延迟），**手动解压是唯一完全不依赖外部状态的路径**，所以手里始终留着它。

> **本机实测记录（2026-09-12，阶段 0 落地时）**：
> - **winget 在本机完全不可用**：`winget --version` 退出码 `-1978335231`（内部错误），`winget source list` 同样失败；而 App Installer 包本身是正常的（1.29.290.0）。所以本机走的是**方式三（手动解压）**。
> - **发现一个网络特性**：这台机器的代理会把**大文件传输中途掐断**（第一次只下到 18.37 MB 就断了，zip 哈希校验失败）。解决办法是**断点续传 + 自动重试**——脚本留在 `.research/dl-hugo-resume.cjs`，能从已下载的部分继续。
> - 最终下载 22,672,703 字节，**SHA256 与上面记录的完全一致**（校验通过）。
> - 安装位置：`%LOCALAPPDATA%\Programs\hugo`（用户目录，**不需要管理员权限**），已追加到**用户 PATH**；新开终端里 `hugo version` 可直接调用。
> - 验证输出：`hugo v0.165.0-76a5e1880ab46688155b02e99bab9be2a6134492+extended windows/amd64 BuildDate=2026-08-12T14:26:28Z`。

> ❓ 疑问：版本用目前最新的版本号，要指定版本，不要直接latest，避免以后有版本冲突
> 
> ✅ **已采纳，而且比你预想的更必要（2026-09-12 核实）**：Blowfish 在自己的配置里声明了版本区间 `min = 0.162.0`、**`max = 0.165.0`**。也就是说装比 0.165.0 更新的 Hugo（当前最新是 0.166.0）**会因不满足主题声明而构建失败**——「用最新版」这条路在 Blowfish 上根本走不通。锁定方案见 **3.5**。

#### 3.4.2 起步方式（推荐第 ① 种：官方脚手架）

| 方式               | 做法                                                                                      | 适合谁                           |
| ---------------- | --------------------------------------------------------------------------------------- | ----------------------------- |
| **① 官方脚手架 CLI（落地首选）** | `npx blowfish-tools new my-blog`（只需 Node，你已装 v24）                    | 官方现在主推的方式，生成的结构**与当前主题版本对齐** |
| ② 官方模板仓库（备选） | 克隆/fork `https://github.com/nunocoracao/blowfish_template` | 配置齐全、开箱能跑；但它的 CI 用的是**较旧的动作版本**，结构也可能滞后于主题（备查用） |
| ③ Git 子模块        | `git submodule add -b main https://github.com/nunocoracao/blowfish.git themes/blowfish` | 想从零搭、且熟悉 Git 的人               |
| ④ Hugo Modules   | `hugo mod init` + `module.toml` 里 import `github.com/nunocoracao/blowfish/v3`           | 需要本地装 Go，**不推荐给你**            |

⚠️ 无论用哪种方式起步，GitHub Actions 里 `actions/checkout` 都必须带 `submodules: recursive`，否则云端构建会报「找不到主题」。官方说明 v2 → v3 完全向后兼容。

**落地时必须先确认一件事（会影响工作流）**：脚手架生成的项目里，主题是**以 git submodule** 引入，还是以 **Hugo Modules** 引入？两者要求不同：

| 主题引入方式 | CI 需要什么 |
|---|---|
| **git submodule**（Blowfish 文档的首选） | `submodules: recursive`（第 14 章工作流已写），**不需要 Go** |
| **Hugo Modules** | CI 必须**额外安装 Go** 才能解析模块（工作流要多一个步骤） |

**这就是我不把第 14 章工作流称为"绝对最终版"的原因之一**：落地时我会先确认脚手架实际用的是哪种，再定工作流——如果是 Modules，我会补上安装 Go 的步骤。

> **本机实测记录（2026-09-12，阶段 0 落地时）**：官方脚手架 `npx blowfish-tools`（v 最新）**在我的执行环境里跑不通**——它会 spawn 子进程并用管道读取 `hugo version` 的结果，而当前沙箱禁止命名管道，于是被它误判为「Hugo is not available」（实际 Hugo 已装好且可用，`--help` 与命令清单都能正常输出）。
> 因此落地时**改用方案里的备选方式**：用 GitHub Desktop 自带的 **git 2.53.0** 执行 `git submodule add --depth 1 -b main https://github.com/nunocoracao/blowfish.git themes/blowfish`，**主题以真正的 submodule 引入并锁定 commit**——结构与第 14 章的工作流完全兼容，也不需要 Hugo Modules（不需要 Go）。
> 若你以后想用官方脚手架：**在你自己的终端里没有沙箱限制，可以正常跑**，想用随时告诉我。

**本方案的最终选择（把关系说清）**：用**官方脚手架 `npx blowfish-tools new`** 生成整个站点骨架，主题**以 git submodule 方式引入**——所以「脚手架给配置 + 主题走 submodule」是**同一套做法**，不冲突。落地时我会：① 确认脚手架实际用的是 submodule 还是 Hugo Modules（见上表）；② 把主题当前的 commit 号记录下来（写进 `docs/versions.md`）。

**关于「跟踪 main」的澄清（这条容易被误解）**：`git submodule add -b main` 看着像"永远跟随最新"，其实不是——submodule 在父仓库里**记录的是一个确定的 commit**。所以 GitHub Actions 构建时拉的是**那个被记录的 commit**，不会因为主题发布新版就自动变（这正是 CI 稳定的原因）。只有你手动执行 `git submodule update --remote --merge` 才会升级，而升级规则见 3.4.5（先本地验证再推）。

**因此需要做的只有一件事**：落地时**把主题当前的 commit 号记录下来**（写进 `docs/versions.md`），万一以后样式坏了可以一键回退到当时的主题版本。**不需要**额外的"锁版本"操作。

#### 3.4.3 配置文件结构

```
config/_default/
├─ hugo.toml            # 站点地址、语言、输出格式（站内搜索索引靠它）
├─ params.toml          # 主题全部开关（首页布局、搜索、统计、文章显示项）
├─ languages.zh-cn.toml # 中文语言配置
├─ menus.zh-cn.toml     # 顶部菜单
├─ markup.toml          # Markdown 渲染（代码高亮、目录等）
└─ module.toml          # 仅 Hugo Modules 方式需要，不要覆盖它
```

- 做法：删掉 Hugo 自动生成的根 `hugo.toml`，从主题的 `config/_default/`（或模板仓库）拷一份进自己的 `config/_default/`；非 Modules 方式必须在 `hugo.toml` 里写 `theme = "blowfish"`。
- **中文**：主题 i18n 提供 `zh-CN.yaml` / `zh-TW.yaml`；站点语言代码按官方写法用小写 **`zh-cn`**（`locale`、`isoCode` 都写 `zh-cn`，Hugo 查找 i18n 文件时大小写不敏感）。

#### 3.4.4 必需功能怎么开（确切参数名）

| 功能             | 参数                                                                     | 写在哪                            | 要点                                                                                                                                                                          |
| -------------- | ---------------------------------------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **站内搜索**       | `enableSearch = true`                                                  | `params.toml`                  | 同时要求 `hugo.toml` 里有 `[outputs] home = ["HTML", "RSS", "JSON"]`；索引**构建时自动生成**，不需要插件；快捷键 Ctrl/⌘+K，底层是 Fuse.js                                                                 |
| **访问统计（本次不做）** | `[umamiAnalytics] websiteid = "..."`                                   | `params.toml`                  | Umami **原生支持**（另支持 Fathom、Seline、Google Analytics）；Cloudflare / 百度统计不原生支持，走 `layouts/partials/extend-head.html` 扩展位                                                         |
| **评论（本次不做）**   | `article.showComments = true`                                          | `params.toml` 或单篇 front matter | ⚠️ Blowfish **不内置任何第三方评论服务**（没有 Giscus/Twikoo/Waline 现成模板）：它只在检测到 `layouts/partials/comments.html` 时才渲染评论。接 Twikoo/Waline **需要我自建这个 partial**（官方预留扩展位）；也可以用 Hugo 内置的 Disqus |
| 首页布局           | `[homepage] layout`                                                    | `params.toml`                  | 可选 `page / profile / hero / card / background / landing / custom`                                                                                                           |
| 顶栏样式           | `[header] layout`                                                      | `params.toml`                  | 可选 `basic / fixed / fixed-fill / fixed-gradient / fixed-fill-blur / floating`                                                                                               |
| 文章头图样式         | `article.heroStyle`                                                    | `params.toml`                  | 可选 `basic / big / background / thumbAndBackground`                                                                                                                          |
| 首页最新文章         | `showRecent` / `showRecentItems` / `showMoreLink` / `showMoreLinkDest` | `params.toml`                  | 控制首页「最近文章」区块                                                                                                                                                                |

#### 3.4.5 主题升级策略

| 安装方式         | 升级命令                                    | 风险                  |
| ------------ | --------------------------------------- | ------------------- |
| Git 子模块      | `git submodule update --remote --merge` | 低                   |
| Hugo Modules | `hugo mod get -u`                       | 低                   |
| 手动复制         | 下载新版覆盖 `themes/blowfish`                | **对主题文件的本地改动会全部丢失** |

原则：**先本地 `hugo server` 看一遍，没问题再推送**。子模块方式下 v2 → v3 无需改任何代码。

#### 3.4.6 你提供的两个参数（一个是 Hugo 核心配置，一个是别的主题的参数）

> 你给出的两行：`hasCJKLanguage = true` 和 `ShowCodeCopyButtons = true`。核实结论：**第一个要加；第二个是 PaperMod 主题的参数，写进 Blowfish 完全无效**（换成 Blowfish 的正确参数名即可达到你想要的效果）。

| 你的写法                         | 判断                                                                                   | 正确写法                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| `hasCJKLanguage = true`      | ✅ **要加**（Hugo 核心的站点配置）                                                               | 写进 `config/_default/hugo.toml`，原样即可                                        |
| `ShowCodeCopyButtons = true` | ❌ **在 Blowfish 里无效**：该参数属于 **PaperMod** 主题，Blowfish 全仓库对它 **0 处引用**；写进去不报错，但也不会有任何按钮 | 改用 Blowfish 的 **`enableCodeCopy = true`**，写进 `config/_default/params.toml` |

##### ① 为什么 `hasCJKLanguage` 对你是「必须」而不是「可选」

Blowfish 默认会显示**字数**和**阅读时间**（`showWordCount`、`showReadingTime` 默认都是开）。而 Hugo 在未开启 CJK 支持时，会把一整段中文当成**一个「词」**。后果非常直观：

| 项目            | 不开 `hasCJKLanguage`    | 开启后                                   |
| ------------- | ---------------------- | ------------------------------------- |
| 字数            | 整篇中文 ≈ **1 个词**        | 按汉字数正确统计                              |
| 阅读时间          | 恒为 **1 分钟**            | 按 CJK 规则 500 字/分钟估算                   |
| 摘要 `.Summary` | 几乎等于**全文**，且「是否截断」判断为否 | 按 `summaryLength`（默认 70）在**段落边界**正经截断 |

也就是说：不开这个开关，你的中文文章页头会一直显示「1 词 · 1 分钟阅读」，列表页摘要退化成全文——**看起来就像博客坏了**。

两个细节：Hugo 是**逐页探测**的（页面里出现汉字/假名/韩文才按 CJK 处理），纯英文文章自动退回普通模式，**混排站点互不影响**；要强制某篇，可在它的 front matter 里写 `isCJKLanguage`。

##### ② 摘要长度要不要调

`summaryLength` 默认 **70**，开启 CJK 后含义变成「约 70 个汉字」，且只在段落结尾截断（不会截半句）。Blowfish 列表页默认**不显示摘要**（`list.showSummary` 默认 false），所以**先用默认值就行**，觉得摘要太短再调大。

##### ③ 你要的「代码复制按钮」正确开法

```toml
# config/_default/params.toml
enableCodeCopy = true
```

附带条件（主题自带配置已满足，不必你操心）：代码高亮必须用 class 方式（`markup.highlight.noClasses = false`）——Blowfish 的 `markup.toml` 已经设好；写错的话按钮不会出现。

##### ④ Blowfish 代码块还能做什么（没有的就是没有，别去找）

| 能力        | 怎么用                                          | 是否有参数                                     |
| --------- | -------------------------------------------- | ----------------------------------------- |
| 复制按钮      | `enableCodeCopy = true`                      | ✅                                         |
| 代码块标题     | 围栏上写 `title="..."`                           | ✅                                         |
| 行号        | 围栏属性 `lineNos` 或 Hugo 的 `[markup.highlight]` | ✅                                         |
| 高亮指定行     | 围栏属性 `hl_lines`                              | ✅                                         |
| 语言标签、代码折叠 | ——                                           | ❌ **没有**这两个参数（`collapse` 是折叠面板短代码的，不是代码块） |

**结论**：`hasCJKLanguage = true` 与 `enableCodeCopy = true` 我都会写进站点配置；`ShowCodeCopyButtons` 请**不要写**，它不会生效。

#### 3.4.7 站点配置里建议一并写上的几项

| 配置项 | 建议值 | 为什么 |
|---|---|---|
| `timeZone` | `"Asia/Shanghai"` | 不写的话部分时间按 UTC 走，文章日期看起来会**差 8 小时** |
| `mainSections` | `["posts"]` | 明确「文章区」就是 `posts/`；否则 `about`、将来新建的 `links` / `projects` 页面可能混进首页文章流 |
| `summaryLength` | `70`（默认，先不动） | 开启 CJK 后约等于 70 个汉字；Blowfish 列表页默认不显示摘要，不必急着调 |
| `hasCJKLanguage` | `true` | 见 3.4.6（不写会出现「1 词 · 1 分钟阅读」） |
| `enableSearch`（`params.toml`） | `true` | 见 3.6 |
| `enableCodeCopy`（`params.toml`） | `true` | 见 3.4.6 |
| `[outputs] home` | 含 `JSON` | 站内搜索索引依赖它（主题默认已含） |
| `[taxonomies]` | 注册 `series` | 想用系列文章就必须注册（见 5.2） |

**关于文章「最后更新时间」（`lastmod`）：两种策略只选一种**

| 策略 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **A（本方案采用）** | 手动在 front matter 写 `lastmod` | 完全可控、不依赖 Git 历史 | 容易忘记更新 |
| B（备选） | 打开 `enableGitInfo = true`（工作流已有 `fetch-depth: 0`） | 自动、无需维护 | 只移动目录或批量格式化也会让时间跳动 |

**不要两种混用**。我们的工作流本来就带 `fetch-depth: 0`，将来想切到策略 B 只需在 `hugo.toml` 加一行——但**阶段一用手动更简单可控**。

### 3.5 Hugo 版本锁定（**这条比表面看起来更重要**）

> 针对你的疑问「要指定版本，不要 latest」——核实结论：**锁版本不是建议，而是必须**，否则主题会拒绝构建。

| 项目            | 结论                                                        | 一手依据                                                             |
| ------------- | --------------------------------------------------------- | ---------------------------------------------------------------- |
| **锁定版本**      | **Hugo 0.165.0 extended**                                 | Blowfish `.github/workflows/pages.yml` 里 `HUGO_VERSION: 0.165.0` |
| **主题声明的版本区间** | `extended = true`、`min = "0.162.0"`、**`max = "0.165.0"`** | Blowfish 根 `config.toml` 的 `[module.hugoVersion]`                |
| 官方模板仓库        | 同样锁 `0.165.0` + `extended: true`                          | `blowfish_template/.github/workflows/pages.yml`                  |
| 当前 Hugo 最新版   | 0.166.0                                                   | Scoop bucket 现状                                                  |

**为什么必须锁**：Blowfish 在主题配置里写了版本**上限**。装了比它新的 Hugo，Hugo 会因为不满足主题声明的区间而**直接报错退出**——不是"可能有兼容问题"，是构建失败。所以在本主题上，「装最新版」是错误的做法。

**本机安装**（见 3.4.1 的命令块，三种方式都给了确切写法）。

**云端必须锁同一个版本**：GitHub Actions 里写 `env: HUGO_VERSION: 0.165.0`，并下载 **extended** 的 Linux 包（`hugo_extended_0.165.0_linux-amd64.deb`——**注意是小写 `linux-amd64`**，`Linux-64bit.deb` 这个写法在 0.165.0 上是 404，实测确认过）。完整工作流见 **第 14 章附录**。

**升级规则**（三个条件同时满足才升）：

1. Blowfish 发布新版、并把它自己的 `max` 提高到新版本之后；
2. 本机换成新版本，`hugo server` 本地看一遍没异常；
3. 再推送，观察 Actions 是否成功。

**永远不要单方面升级 Hugo**——这是本方案里唯一一条"版本必须成套"的依赖。

**为什么要每季度看一眼（重要，补于 v1.9）**：Blowfish 的版本窗口是**滚动**的——它有个自动机器人，把 `max` 跟着 Hugo 最新版上移、`min` 设为「最新版往前推 3 个小版本」。所以 `max = 0.165.0` 只是**2026-09 这个时点**的值，过几个月可能就变成 0.166、0.167……

因此策略是「**成对升级**」：**每季度看一次 Blowfish 的 release 或 `config.toml`，如果它的 `max` 已上移，就把「Hugo + 主题 commit」两个一起升级**（步骤同上面的三条规则：先本地验证，再推）。

- **不升也不会坏**：因为我们**同时锁了主题 commit**，那个 commit 里声明的区间永远是 0.162.0–0.165.0，与 Hugo 0.165.0 永远匹配——不存在"装不上"，只是会逐渐落后于主题的新功能与新修复。
- **但两者必须一起动**：如果你哪天手动把主题升到新版、却没升 Hugo，就会撞上版本校验而构建失败。

### 3.6 中文站内搜索（你特别提的需求，我实测过了）

> 结论先给：**Blowfish 默认的中文搜索「能用但很脆」**——我用主题自带的 Fuse.js 做了实测，默认配置下**只有连续的中文子串能搜到**。要做到像样的中文搜索，需要覆盖主题的两个文件（改动很小，**不需要换搜索库**）。

#### 3.6.1 默认实现是什么

| 项目           | 事实                                                                                                             |
| ------------ | -------------------------------------------------------------------------------------------------------------- |
| 引擎           | **Fuse.js v7.5.0**（完整版，随站点一起打包，**纯客户端、不走任何外部服务**）                                                              |
| 开启方式         | `params.toml`：`enableSearch = true`；并要求 `hugo.toml` 的 `[outputs] home` 含 `JSON`（主题默认已含）                        |
| 索引文件         | 构建时自动生成站点根目录的 `index.json`（模板 `layouts/_default/index.json`）                                                   |
| 索引**包含**的字段  | `title`、`summary`、`content`（**全文，不截断**）、`section`、`date`、`permalink`、`type`                                    |
| 索引**不包含**的字段 | **`tags` 和 `categories` 完全不进索引**                                                                               |
| Fuse 默认参数    | `threshold: 0.0`（**零容错**）、`ignoreLocation: true`、`keys` 权重 title 0.8 / summary 0.6 / content 0.4 / section 0.2 |

#### 3.6.2 默认配置下中文的实际表现（实测数据）

| 你输入                | 结果                    | 原因                                     |
| ------------------ | --------------------- | -------------------------------------- |
| `搜索`、`中文搜索`        | ✅ 命中                  | 连续子串                                   |
| `搜索功能`             | ❌ 搜不到                 | `threshold: 0.0` = 零容错，必须是正文里的**连续片段** |
| `中文 搜索`（**中间带空格**） | ❌ 搜不到                 | 默认不按空格切词，整串被当成一个词                      |
| 单个汉字（如 `搜`）        | ⚠️ 满屏噪声 → **按 3.6.3 加上 `minMatchCharLength: 2` 即可解决**               | `minMatchCharLength: 1`                |
| 超过 32 字的查询         | ⚠️ 被切成 32 字块，命中任一块即算中 | 引擎行为                                   |
| 结果相关性排序            | ❌ 没有排序                | `threshold: 0` 时所有命中分数都是 0，基本按站点页序返回   |

**还有一个对你影响很大的点**：因为**分类和标签不进索引**，你搜「群晖」这种标签名，只有正文里恰好写过「群晖」才搜得到——对以分类/标签组织内容的博客，这是真实缺陷。

#### 3.6.3 解决方案（**分两步走，先做减法**）

Hugo 的资源查找规则是**项目文件优先于主题文件**，所以在我们自己的项目里放同名文件即可覆盖，主题升级也不冲突。

> **先纠正一个常见误解**：有种说法是「Fuse 默认的分词正则 `[\p{L}\p{M}\p{N}_]+` 已经能处理中文」。这个理解**不准确**——那个正则按"非字母数字"切分，而**汉字本身属于 `\p{L}`**，所以一整串连续中文会被当成**一个巨大的 token**，等于没有分词。这也是下面第二步要自己写分词器的原因。

##### 第一步（改动最小，先只做这些）

1. 打开 `enableSearch`（`params.toml`）；
2. **覆盖 `layouts/_default/index.json`**：把 `tags`、`categories` 也写进索引，并在 `search.js` 的 `keys` 里加上它们（建议权重 tags 0.5 / categories 0.3）——这样**搜标签名/分类名就能找到文章**；
3. 只加**一个**参数：`useTokenSearch: true`（**先不写自定义分词函数**）。

然后**立刻按 3.6.5 的四类查询实测**。

这样做的理由：改动面最小、只覆盖一个文件、主题升级时要 diff 的东西最少，**回退成本最低**。

##### 第二步（只在第一步不达标时才做）

如果"带空格的多词"仍然搜不到，再加上 `threshold` 与**自定义中文分词**（改的是同一个 `search.js`）：

```js
threshold: 0.35,          // 给一点容错，"搜索功能"这类非连续词也能命中
minMatchCharLength: 2,    // 至少匹配 2 个字：解决单字查询满屏噪声的问题
useTokenSearch: true,     // 启用 token 搜索
tokenMatch: "all",
tokenize: (text) =>       // 用浏览器自带的中文分词器切词
  Intl.Segmenter
    ? Array.from(new Intl.Segmenter("zh", { granularity: "word" }).segment(text),
        (s) => (s.isWordLike ? s.segment : null)).filter(Boolean)
    : text.split(/\s+/)   // 老浏览器兜底：退化成默认行为，不会报错
```

实测效果：`中文 搜索`、`hugo 配置` 这类**带空格的多词查询可以跨词、跨字段命中**（这正是默认配置下完全搜不到的场景）。

#### 3.6.4 代价与注意事项（必须让你知道）

1. **覆盖这两处后，主题升级不会自动更新它们**。升级主题后要留意主题是否改动了同名文件——我落地时会在两个文件顶部写清来源与改动说明，方便日后比对。
2. 中文分词依赖浏览器的 `Intl.Segmenter`（现代 Chrome / Edge / Safari / Firefox 均支持）；上面的代码带了兜底，老浏览器只是退化成默认行为，**不会报错或白屏**。
3. 索引是**全文**（含正文全部文字），文章多了以后 `index.json` 会变大（上百篇会有几 MB）。真到那时再考虑 Pagefind 等方案——**现在不需要**。
4. 搜索是**纯客户端**的：不需要服务器、不产生费用，代价是首次搜索前要下载索引文件。
5. **上面三个参数是 Fuse.js v7 的真实选项，不是猜测**：`useTokenSearch`、`tokenMatch`、`tokenize` 都是 Fuse v7 新增的官方配置，**前提是主题打包的是 Fuse 的完整版**——我已核实 Blowfish 内置的正是**完整版**（含 `TokenSearcher`），并已用主题自带的那份 Fuse 实测跑通。⚠️ 但若哪天主题换成精简版或换掉搜索库，这几个参数会**静默失效**（不报错、只是没效果）——所以**验收只能靠实测，不能靠"配置写了"**（3.6.5 的四项实测就是为此设计的）。
6. **回退触发条件（提前定好，免得到时候纠结）**：出现以下任一情况就换方案——① 按上面配置后，四类中文查询仍有 ≥2 类搜不到；② `index.json` 涨到 1~2 MB 以上（大约几十篇长文之后）。回退顺序：先试**自定义搜索逻辑**（把查询按空格 + 中文分词切开，对 title / tags / categories / summary / content 分权重匹配，标题权重最高），再考虑 **Pagefind**（构建期生成分片索引、中文友好、按需加载）。
7. **自定义 `tokenize` 函数有两个已知限制**：① 它**不能与 Fuse 的 worker 模式共用**（所以别同时开启相关配置）；② `threshold` 与它的配合效果**不保证跨 Fuse 版本稳定**——这正是 3.6.3 要"分两步走、第二步才动它"的原因。
8. **`minMatchCharLength: 2` 会让单字查询"什么都没有"**：这是**预期行为**（就是为了消灭满屏噪声），但用户可能以为坏了。落地时我会把搜索框提示文案改成「至少输入 2 个字」（Blowfish 界面文案走 i18n，项目里可以覆盖；若该文案不可覆盖，我会改为保留 `minMatchCharLength: 1` 并在方案里写明取舍）。
9. **升级主题时的额外判断标准**：如果你覆盖的那个文件与新版主题的同名文件**差异超过 50 行**，就**放弃覆盖**、直接切 Pagefind——维护成本已经超过收益。

#### 3.6.5 落地与验收标准

落地时我会：打开 `enableSearch` → 覆盖上面两个文件 → 用**你自己的真实文章**分别测这四类查询：**① 连续词 ② 带空格的多词 ③ 标签名 ④ 分类名**。**四项全部出结果，这项才算验收通过**（已同步为第 6 章阶段 5 的验收标准）。

> 本次实测用的脚本保留在 `.research/fusetest.cjs`（跑 Node 即可复现上表结论），你可以自己复核一遍再决定信不信我这套判断。

---

## 4. 域名与托管方案（重点）

### 4.1 推荐拓扑：GitHub Pages 做源站 + Cloudflare 做前置

Cloudflare DNS 里加一条记录：

```
类型: CNAME
名称: blog
目标: wxg007post.github.io
代理状态: 先「仅 DNS」（灰色云），证书签发成功后再切成「已代理」（橙色云）
```

HTTPS 的正确顺序（**顺序错了会踩证书坑**）：

1. 先让记录是**灰色云（DNS only）**，GitHub Pages 后台绑定 `blog.wxgg.eu.cc`，等 GitHub 自动签发 Let's Encrypt 证书（通常几分钟到几小时，DNS 校验）。
2. 证书生效、`https://blog.wxgg.eu.cc` 能打开后，再去 Cloudflare 把这条记录**切成橙色云**，SSL/TLS 模式设为 **Full (strict)**，并打开 **Always Use HTTPS**。
   同时别忘了 GitHub 侧那一半：仓库 `Settings → Pages` 里的 **Enforce HTTPS** 复选框也要勾上——它负责在"还没走 Cloudflare 代理"的阶段就把 http 请求跳成 https（第 15.3 的验收项依赖它）。
   **HSTS 第一版先别开**：一旦开启而证书又出问题，浏览器会直接强制拒绝访问，排查会更麻烦；等站点稳定跑几个月再说。
3. 若暂时打不开但急着加速，可临时用 **Full（非 strict）**，等 GitHub 证书下来再切回 strict；**不要用 Flexible**，否则容易出现重定向循环。

**关于 `static/CNAME`：本方案默认不放这个文件。** 用 **GitHub Actions 方式**部署时，自定义域名是**在仓库 `Settings → Pages` 里配置、由 GitHub 自己管理**的；仓库里那份 `CNAME` 文件是**旧的分支部署方式**才需要的东西。更实际的理由：**在 `github.io` 上做临时验证时，仓库里如果躺着一份写着 `blog.wxgg.eu.cc` 的 CNAME，反而会干扰测试**。所以顺序是：**先在 Pages 设置里填域名 → 按下面 4 项验证构建产物 → 不需要再往仓库里加 CNAME**。将来若真改成"从分支部署"，再补这个文件也不迟。

**验证清单（这步很多人跳过，然后遇到白板或搜索失效）**：域名生效后，打开线上页面查看源代码，确认 4 件事：

1. CSS / JS 的地址是 `https://blog.wxgg.eu.cc/...`（**不是** `wxg007post.github.io/blog/...`）；
2. `<link rel="canonical">` 指向 `https://blog.wxgg.eu.cc/...`；
3. `https://blog.wxgg.eu.cc/index.json` 能打开（搜索索引）；
4. `https://blog.wxgg.eu.cc/sitemap.xml` 和 `rss.xml` 能打开。

**若第 1、2 项不对**：说明 `baseURL` 没跟上——把 `config/_default/hugo.toml` 里的地址改成 `https://blog.wxgg.eu.cc/`（**结尾斜杠必须有**），或在工作流里显式写 `--baseURL "https://blog.wxgg.eu.cc/"`，然后重新触发一次部署。

### 4.2 关于 Cloudflare 的合规提醒

Cloudflare 免费版是 CDN，用它加速自己 GitHub Pages 上的静态博客属于常见用法，但要注意：**不要**把它变成代理翻墙/反代视频站之类的用途，免费账号被滥用会被限制甚至清退。博客这种低流量静态站，完全没问题。

### 4.3 备案问题

- 托管在 GitHub Pages（境外 IP）**不需要 ICP 备案**，域名解析在 Cloudflare 也不需要。
- 但如果将来为了国内速度把源站换成国内服务器/CDN（如腾讯云 EdgeOne 国内节点），就**必须备案**，而 `eu.cc` 这类免费域名通常**不支持备案**。所以本方案长期走"境外托管 + Cloudflare 加速"路线最省事。

### 4.4 如果国内访问还是慢（备选拓扑）

| 方案    | 做法                                                                     | 代价                       |
| ----- | ---------------------------------------------------------------------- | ------------------------ |
| 双托管镜像 | 同一份代码同时部署到 GitHub Pages + Cloudflare Pages（或腾讯云 EdgeOne Pages），国内走加速线路 | 多一套部署配置，两处要同步            |
| 直接换托管 | 以 Cloudflare Pages 为主（构建也在 CF，免费额度够个人用）                                | 偏离你"部署到 GitHub Pages"的初衷 |
| 保持现状  | 只做 GitHub Pages + CF 代理 + 图片压缩                                         | 部分地区、部分时段仍会慢             |

**建议：先按主方案上线，用一两个月真实数据（Cloudflare 后台能看到各地延迟）决定要不要上镜像。**

### 4.5 仓库策略

| 方案               | 说明                                                               | 适合                |
| ---------------- | ---------------------------------------------------------------- | ----------------- |
| **公开仓库 + Pages** | **已选定**：最简单，免费账号的 Pages 要求站点仓库公开                                 | 首推，先跑起来           |
| 私有源码 + 公开输出      | 源码放私有仓库，Actions 构建后推到公开的 `wxg007post.github.io` 仓库（**本次未采用**，备查） | 想让"写作过程/草稿"不公开时再上 |

> ⚠️ 公开仓库意味着**你写下的每一个字都是全网可见并可被永久存档的**，删掉也可能已被 fork。第 7 章的脱敏必须做。

### 4.6 仓库命名与站点地址（**有个必须提前避开的坑**）

你的 GitHub 用户名是 `wxg007post`，仓库怎么命名会决定站点地址：

| 类型       | 仓库名                    | 未绑域名时的地址                             | 说明                         |
| -------- | ---------------------- | ------------------------------------ | -------------------------- |
| **用户站点** | `wxg007post.github.io` | `https://wxg007post.github.io/`      | 地址直接在根目录，**不会出现子路径问题**，最省心 |
| 项目站点     | 任意，例如 `blog`           | `https://wxg007post.github.io/blog/` | 仓库名好看，但地址带 `/blog/` 子路径    |

**为什么这件事重要**：Hugo 的 `baseURL` 必须和站点真实地址一致，否则 CSS、图片、搜索索引会全部 404——现象是**页面能打开但样式全丢**，这是新手最常遇到的「白板」问题。

- 绑定 `blog.wxgg.eu.cc` 之后，两种类型的最终访问地址都是域名根目录，`baseURL` 统一写 `https://blog.wxgg.eu.cc/`（**结尾的斜杠不能省**）。
- 本地预览不受这个问题影响：`hugo server` 会自动把站点地址替换成 `localhost`；但**推送到 GitHub 的那次构建必须用正确的 `baseURL`**（写在 `config/_default/hugo.toml` 里，工作流中也可以用 `--baseURL` 覆盖）。
- 顺序建议：**先绑域名、把 `baseURL` 设成域名，再开始发内容**。若想先在 `github.io` 上验证一次，就临时把 `baseURL` 设为对应的 `github.io` 地址，验证完改回来（每次改都要重新构建）。
- 我的建议：**用项目站点（仓库名 `blog`）**——仓库名清晰，域名紧接着就绑，这个「白板期」很短；想彻底避免这类坑就用用户站点（仓库名 `wxg007post.github.io`）。

**已定（2026-09-12）：采用项目站点，仓库名 `blog`。** 按上面的顺序操作即可——要么一开始就把 `baseURL` 写成 `https://blog.wxgg.eu.cc/`，要么先写成 `https://wxg007post.github.io/blog/` 验证一次、绑好域名后再改回来并重新构建。

### 4.7 Cloudflare 缓存规则（**不加这条，会出现"文章发了但搜不到"**）

静态站套上 CDN 后有个典型症状：**新文章明明已经上线，站内搜索却搜不到**——因为 `/index.json`（搜索索引）被 Cloudflare 缓存住了。

**用哪个功能配**：用 **Cache Rules（缓存规则）**，**不要用 Page Rules**——免费版 Page Rules 只有 10 条且 Cloudflare 已主推 Cache Rules。位置：Cloudflare 后台 → 你的域名 → **缓存 → Cache Rules → 创建规则**。规则如下：

| 路径 | 缓存策略 | 原因 |
|---|---|---|
| `/index.json` | **不缓存，或最多 5 分钟** | 搜索索引用它；缓存久了新文章搜不到 |
| `/sitemap.xml`、`/rss.xml` | 短缓存（≤ 5 分钟） | 让搜索引擎与订阅者及时看到新内容 |
| `/images/*`、`/css/*`、`/js/*`、字体 | 可以长缓存 | 静态资源，缓存越久访问越快 |
| HTML 页面 | **不要长缓存**，也不建议开 Cache Everything | 否则你改完文章，访客长时间看到旧版 |

另外建议在 Cache Rules 里加一条「**带查询字符串的请求绕过缓存**」（Bypass cache on query string）——否则 `?v=123` 这类预览参数也会命中旧缓存，你会以为"改了没生效"。

两条排查经验：**发完文章想立刻验证，按 Ctrl+F5 强刷**（排除浏览器缓存干扰）；Cloudflare 后台的「清除缓存」能一键清全部，是排查「内容不更新」的第一手段。

### 4.8 域名风险（**这是本方案最大的单点风险，务必看完**）

`wxgg.eu.cc` 是**免费二级域名**：`.cc` 是国家顶级域，而 `eu.cc` 是域名注册商 GNAME 提供的**免费三级域**。它的"免费"是有条件的，规则和普通域名**不一样**：

| 事实（2026-09 核实） | 对你的影响 |
|---|---|
| 注册免费、续期也免费，但**必须手动续** | 没有"到期自动扣款"这回事 |
| 免费续期**只能在 GNAME 的活动页面手动领券提交**；在"我的域名"列表里操作、或开启自动续费，**都不生效** | 忘了就真的会过期 |
| **只能在到期前 90 天内**操作 | 太早提交不了，需要一个提醒窗口 |
| 双字母 / 三字母 / 品牌词属于溢价域名，**不能用免费券** | 想换个好记的名字就得花钱 |
| 域名一旦过期释放，**谁都可以注册走** | 你的全部外链、搜索引擎收录、读者订阅的 RSS 会一起失效，**且无法找回** |

**应对措施（前两项落地时我会帮你做好）**：

1. **把到期日写进 `docs/versions.md`**，并设日历提醒：**到期前 100 天开始、每两周提醒一次**（留出 90 天窗口打开前的提前量）。
2. **每半年做一次"续期演练"**：登录 GNAME 活动页，确认免费续期入口还在——政策会变，演练能提前发现问题。
3. **长期预算里留一条备选**：花约 **¥70–90/年**买个 `.com` 之类的正式域名作为主域，把 `blog.wxgg.eu.cc` 做 301 跳转。这样即使免费域名丢了，你还有一个能长期持有的域名（第 8 章备注已写明）。
4. **在 GitHub Pages 里做域名验证**（`Settings → Pages → Verified domains`，按提示加一条 TXT 记录）：防止别人把你的域名挂到他自己的 Pages 站点上。

---

## 5. 内容体系与写作规范

### 5.1 目录结构（Blowfish 版）

```
my_blog/
├─ content/
│  ├─ posts/
│  │  └─ synology-jellyfin-hardware-transcode/   ← 一篇文章一个文件夹（page bundle）
│  │     ├─ index.md                              ← 正文
│  │     └─ feature.webp                          ← 头图 / 列表缩略图（文件名见 5.2 说明）
│  └─ about/index.md                              ← 「关于我」
├─ config/_default/                               ← 站点配置目录（注意不是根目录的 hugo.toml）
│  ├─ hugo.toml                                   ← 站点地址、语言、[outputs]（站内搜索靠它）
│  ├─ params.toml                                 ← 主题开关：首页布局 / 搜索 / 统计 / 文章显示项
│  ├─ languages.zh-cn.toml                        ← 中文语言配置
│  ├─ menus.zh-cn.toml                            ← 顶部菜单
│  ├─ markup.toml                                 ← 代码高亮、目录等渲染设置
│  └─ module.toml                                 ← 仅 Hugo Modules 方式需要
├─ static/
│  ├─ images/                                     ← 通用图片（头像、封面等）
│  └─ CNAME（**通常不需要**，见 4.1）                                       ← 用 Actions 部署时域名在仓库 Pages 设置里配，一般不必有这个文件
├─ archetypes/default.md                          ← 新建文章模板（`hugo new` 时套用）
├─ themes/blowfish/                               ← 主题（git submodule，**commit 已记录**，见 3.4.2）
├─ .github/workflows/deploy.yml                   ← 自动构建发布流水线
└─ .gitignore
```

> 注意：Blowfish 的配置在 `config/_default/` 下的**多个文件**里，不是根目录一个 `hugo.toml`；用官方模板仓库起步的话这些文件已经齐全。

### 5.2 文章模板（Front Matter，字段已按主题源码核对）

```yaml
---
title: "群晖 Docker 部署 Jellyfin 并开启硬件转码"
date: 2026-09-12T20:30:00+08:00
lastmod: 2026-09-12
draft: true                                   # 新建文章默认草稿：脱敏检查通过后再改成 false 发布
slug: "synology-jellyfin-hardware-transcode"   # 决定网址：/posts/<slug>/
description: "一句话说清这篇文章解决了什么问题（进搜索结果和分享卡片）"
categories: ["折腾记录"]                        # 分类只有两个：折腾记录 / 攻略（见 5.3）
tags: ["NAS", "Docker", "Jellyfin", "硬件转码"]  # 标签可自由新增，注意写法统一（见 5.3）
featureimage: "feature.webp"                   # 头图 / 列表缩略图；留空则主题自动查找
showHero: true                                 # 正文顶部是否显示大图
heroStyle: "big"                               # basic / big / background / thumbAndBackground
showTableOfContents: true                      # 右侧目录（Blowfish 的字段名不是 toc）
showTaxonomies: true                           # 文章头部显示分类 / 标签徽章
series: []                                     # 系列文章用，需先在 [taxonomies] 注册
series_order: 0
---
```

**几个容易踩的坑（均按主题源码核对）**：

- **目录开关**是 `showTableOfContents`，**不是** Hugo 常见的 `toc`；写错不会报错，只是没效果。
- **头图**用 `featureimage` 指定；也可以不写，把图片按文件名放进文章目录，主题自动按 `*feature*` → `*cover*` → `*thumbnail*` 的顺序查找。**列表页缩略图和文章顶部大图用的是同一张图**。
- **网址**由 `slug` 决定（官方示例：`slug: "front-matter"` → `/docs/front-matter/`），不需要额外配 `[permalinks]`；`url` 字段是 Hugo 的绝对覆盖字段，本主题未使用，**建议不要用**，以免以后换主题时链接失效。
- **系列文章**用 `series` + `series_order`，且必须先在 `config/_default/hugo.toml` 的 `[taxonomies]` 里注册 `series`，否则不显示。
- 其他可用开关（都有全局默认值，可在 front matter 里逐篇覆盖）：`showDate`、`showDateUpdated`（配合 `lastmod`）、`showReadingTime`、`showWordCount`、`showBreadcrumbs`、`showEdit`、`showPagination`、`showSummary`（列表页摘要）、`showComments`、`excludeFromSearch`。
- **日期不要写未来时间**：Hugo 默认**不构建** `date` 晚于构建时间的文章（会静默不显示，很难排查）。发布控制**只用 `draft`**，不要去开 `buildFuture`。
- **front matter 字段名区分大小写**：`featureimage`、`showHero`、`heroStyle`、`showTableOfContents`、`showTaxonomies`、`series_order` 这些字段写错**不会报错**，只是效果不出现（比如头图不显示）。落地时我会逐项实测一遍再交付模板。

### 5.3 分类与标签体系（已按你的方案定稿）

**分类（Categories，就 2 个；一篇文章只归 1 个，必选）**

| 分类 | 放什么 |
|---|---|
| **折腾记录** | NAS、Docker、OpenWrt / ImmortalWrt、OpenClash、Tailscale、软路由、AI 工具……所有技术折腾与踩坑总结 |
| **攻略** | **更宽的类（2026-09-12 你确认）**：游戏攻略 + **选购/选型攻略** + 方案对比 + 避坑指南——凡是「**面对多个选项，给出结论或建议**」的内容都归这里 |

**分类判定规则（一篇文章只归 1 个；拿不准时问自己一句话）**

> **读者读完这篇，主要是「能照着做」，还是「能做出选择」？**
> 能照着做 → **折腾记录**；能做出选择 → **攻略**。

| 文章例子 | 归哪类 | 为什么 |
|---|---|---|
| 群晖 Docker 部署 Jellyfin 并开硬解 | 折腾记录 | 过程型：把东西跑起来 |
| OpenClash 分流规则排障实录 | 折腾记录 | 排障过程 |
| Tailscale 子网路由怎么配 | 折腾记录 | 配置过程 |
| 2026 家用 NAS 怎么选：飞牛 vs 群晖 vs 威联通 | **攻略** | 选型结论型 |
| 软路由硬件怎么选：N100 vs J4125 | **攻略** | 选型结论型 |
| 某游戏开局路线 / BOSS 打法 | **攻略** | 游戏攻略 |
| 我的家庭网络拓扑（边搭边讲选型理由） | 折腾记录 | **边界案例**：过程和选型都有时归"折腾记录"，选型部分用标签标记 |

**边界案例怎么处理**：一篇文章如果「搭建过程」和「选型理由」都有，**归折腾记录**（主体是过程），选型部分用标签补充即可，**不要为分类纠结**。

**标签（Tags，可多选，可随时增加）——你的初始清单**

`NAS` ｜ `飞牛` ｜ `Docker` ｜ `OpenWrt` ｜ `ImmortalWrt` ｜ `OpenClash` ｜ `Tailscale` ｜ `软路由` ｜ `AI` ｜ `工具`

> 候选标签（用到再加，新增是零成本）：`选型`、`避坑`、`群晖`、`威联通`、`硬件`、`游戏`、以及具体游戏名……

**两条使用规则（防止标签越用越乱）**

1. **泛指与专指可以并存，但含义要分清**：`NAS` 是泛指类别、`飞牛` 是具体系统——写飞牛的文章**两个都打是对的**（读者可能搜任一个）；同理 `软路由`（硬件角色）与 `OpenWrt` / `ImmortalWrt`（系统）也可以并存。但**别给同一篇文章堆语义重复的标签**（例如同时打 `OpenWrt` 和 `OpenWrt 固件`）。
2. **同一概念只用一种写法**：英文名保持原样大小写（`Docker`、`OpenWrt`、`ImmortalWrt`、`Tailscale`、`OpenClash`），中文不加空格标点。**绝不能**出现 `docker` / `Docker`、`openwrt` / `OpenWrt` 这样的两套写法——Hugo 会把它们当成**两个不同标签**，标签页会被拆散。
3. **一篇文章最多 5 个标签；"只出现过一次"的标签每季度清理一次**。标签是检索工具、不是陈列柜——`AI`、`工具`、`软路由` 这类太泛的标签打多了会变成垃圾筐，标签页也就没人看了。（清理时**不机械删除**：游戏名这类"只出现一次但确实有意义"的标签应当保留，我会逐个给判断。）

**你的问题「后期能不能调整」——能，但要分清三种操作的成本**

| 操作 | 成本 | 说明 |
|---|---|---|
| **新增**分类或标签 | **零成本，随时加** | 写进 front matter 就自动生成页面，无副作用 |
| **改名** | 低，但会断链接 | 标签/分类页的网址会跟着变（`/tags/软路由/` → `/tags/新名/`）；**文章本身的网址不受影响**（由 `slug` 决定）。真要改名，可在 `content/tags/<旧名>/_index.md` 里写 `aliases` 让旧链接跳转（执行前我会先验证一遍） |
| **删除** | 需要收拾 | 得把所有文章里的引用一起清掉，否则会留下空标签页 |

**结论：大胆加、谨慎改、尽量别删。** 建议你把新想到的标签随时告诉我，我定期帮你收敛一次标签表（重复的、近义的、大小写不一致的都挑出来）。

**还需配套做一件事**：分类页和标签页虽然会自动生成，但**顶部菜单不会自动出现**——要在 `config/_default/menus.zh-cn.toml` 里手工加 `/categories/` 和 `/tags/` 两个入口（落地时我加好）。

> ❓ 疑问：
> 
> 1.分类目前只要两个：“折腾记录”和“攻略”，后期是否可以根据我的需要再做调整？
> 
> 2.标签：NAS、飞牛、Docker、OpenWrt、ImmortalWrt、OpenClash、Tailscale、软路由、AI、工具，后期是否可以根据我的需要再做调整？
>
> ✅ **两个问题一起回答（2026-09-12）**：**① 已按你的思路定稿**——分类就 2 个（折腾记录／攻略），标签用你列的 10 个。这个方向**比我原来的建议更符合惯例**：**分类少而稳定，当导航用；标签多而灵活，当检索用**。**② 后期完全可以调整**，但三种操作成本不同：**新增零成本（随时加）／ 改名会断标签页链接（可用 `aliases` 补救）／ 删除要把引用收拾干净**。详见上面「三种操作的成本」表。

### 5.4 三种文体的固定骨架

| 文体      | 骨架                                  | 用途          |
| ------- | ----------------------------------- | ----------- |
| **教程型** | 目标 → 前提条件 → 步骤（带命令/截图）→ 验证方法 → 如何回滚 | 部署教程、配置教程   |
| **踩坑型** | 现象 → 排查过程 → 根因 → 解决方案 → 如何避免        | 你的经验总结，最有价值 |
| **攻略型** | 结论先行 → 数据/表格 → 分情况建议 → 更新日期与版本号     | 游戏攻略、选型建议   |

> 你平时用 AI 写小工具，也可以让 AI 按上面的骨架，把你零散的调试过程整理成一篇结构完整的文章，再自己核对技术细节——这是产出效率最高的一条路。

### 5.5 图片规范

- 格式统一 **WebP**，宽度 ≤ 1600px，单张 < 300KB（截图工具都能转）。
- 图片与文章同目录（`index.md` + `feature.webp` + `step1.webp`），正文里用相对路径引用；头图固定用 `feature.webp`（见 5.2）。
- 文件名用英文小写加连字符，别用中文名（避免各种路径编码问题）。
- **仓库体积红线：超过 300MB 就启动 Cloudflare R2 迁移**。超过之后 `git clone`、Actions 构建、本地同步都会明显变慢。（Hugo 0.165 是否支持 AVIF 编码，落地时我会确认一次；若支持，将来迁 R2 时可以直接上 AVIF，比 WebP 再小一截。）

### 5.6 写作与发布流程（最终形态）

**编辑器怎么选（都行，关键是图片引用格式）**：

| 编辑器 | 情况 |
|---|---|
| **MarkText**（v0.19.1） | ❌ **本机实测不合格（2026-09-12）**：插入图片写成了**绝对路径** `![](D:\<哈希>.webp)`，而且图片被存到 **`D:\` 根目录**、文件名是 64 位哈希——既会导致线上裂图，文件也没法管理。**已放弃** |
| **Obsidian**（免费）✅ **已选定** | 功能最全，但要改下面 3 个设置，否则会写 wiki 链接导致线上裂图 |
| **Typora**（收费，约 $14.99） | 中文用户多、图片相对路径处理成熟；对应设置是「图片 → 优先使用相对路径」+「复制图片到 ./${filename}」 |

**若用 Obsidian，开工前必做这 3 个设置**：

| Obsidian 设置项 | 改成 |
|---|---|
| 设置 → 文件与链接 → **使用 Wiki 链接** | **关闭** |
| 设置 → 文件与链接 → **新链接格式** | **相对路径**（Relative path to file） |
| 设置 → 文件与链接 → **附件默认位置** | **与当前文件相同的文件夹** |

这样插图会写成 `![步骤一](step1.webp)`，Hugo 才能正常渲染。**统一规则（与编辑器无关）：图片与 `index.md` 放在同一个文件夹，引用写相对路径。** 万一编辑器写错，第 7 章的发布前检查会做**坏图检测**兜底。

> **提交身份（✅ 已解决，2026-09-12）**：GitHub 靠**邮箱**把提交关联到账号。已在 GitHub Desktop → Options → Git → Author 里设为 `Wang xg <294689316+wxg007post@users.noreply.github.com>`（GitHub 官方的**不公开邮箱**格式：既正确关联账号，又不暴露真实邮箱）；已复查 `~\.gitconfig` 与 `git config --global` 两端一致，且无环境变量或系统级配置覆盖。

**日常流程**：

1. 用模板新建文章（模板里已经是 `draft: true`，标题/分类/标签都是填空）。
2. 写内容，图片拖进同一个文件夹（Obsidian 会用相对路径引用）。
3. 本地预览：`hugo server -D`，浏览器 `http://localhost:1313` 实时看效果。
4. **跑一次 `发布前检查.cmd`**（启动器，实际逻辑在 `.ps1`；脱敏 + 大图扫描，见第 7 章）。
5. 把 front matter 里的 `draft: true` 改成 `false`。
6. 发布：GitHub Desktop 点 **Commit → Push**。
7. 等 1~2 分钟，Actions 出现绿色勾 = 网站已更新；按 **Ctrl+F5** 强刷查看。

**关键理念：本地不构建也能发布，构建交给云端。**

---

## 6. 落地路线图（分阶段，含验收标准）

| 阶段                    | 做什么                                                                                                                                                                                             | 验收标准                                                                      | 预计耗时         |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------ |
| **0 准备**              | 确认 GitHub 账号；安装 **GitHub Desktop**（自带 Git，图形化）；安装 Markdown 编辑器（推荐 Obsidian，免费）；安装 **Hugo extended 0.165.0**（**必须锁版本**，见 3.5）：本机 winget 不可用，走**手动解压 zip + 校验 SHA256**（✅ 已完成，见 3.4.1 实测记录） | **全部通过（2026-09-12）**：Hugo 0.165.0 extended ✅ / Git 2.53.0 ✅ / GitHub Desktop 已登录 ✅ / 用户名 `wxg007post` 已确认（github.com 返回 200）✅ / Obsidian 已装，3 项设置读到实际值正确（`useMarkdownLinks=true`、`newLinkFormat=relative`、`attachmentFolderPath=./`），插图自测写成相对路径 ✅ | 0.5 小时       |
| **1 本地跑通**            | 用 Blowfish 官方模板仓库初始化站点，改 `config/_default/` 里的站点标题、菜单、头像，语言设为 `zh-cn`                                                                                                                           | 浏览器 `http://localhost:1313` 能看到自己博客首页                                     | 1~2 小时       |
| **2 上线 GitHub Pages** | 建仓库、推代码、加 GitHub Actions 工作流、仓库 Settings → Pages 选 GitHub Actions 作为源                                                                                                                           | 能打开对应地址（用户站点 `wxg007post.github.io`；项目站点则带 `/blog/` 子路径），且**样式正常、不是白板**   | 0.5~1 小时     |
| **3 绑定域名**            | Cloudflare 加 CNAME、GitHub 填自定义域名、按 4.1 的顺序处理 HTTPS、切橙色云                                                                                                                                         | `https://blog.wxgg.eu.cc` 打开正常，小锁头有效                                      | 0.5 小时 + 等证书 |
| **4 内容起步**            | 按 5.3 定好分类；用三种骨架各写 1 篇；补「关于我」和首页介绍                                                                                                                                                              | 首页有 3 篇以上真实文章，分类/标签页可用                                                    | 视写作量         |
| **5 增强**              | 打开站内搜索（`enableSearch` + `[outputs]` 加 JSON，并按 3.6 校准**中文搜索**）、RSS 与 sitemap、SEO 描述、手机端适配检查（**评论、统计、图床按你的决定不做**）                                                                                 | 手机端打开正常、**搜中文能出结果（连续词、带空格的多词、标签名、分类名 四类全过，见 3.6.5）**                      | 2~4 小时       |
| **6 长期维护**            | 每月看一次 Actions 是否正常、每季度更新主题（先本地验证再推）、仓库镜像备份到 NAS                                                                                                                                                 | 有固定的备份习惯                                                                  | 每月 10 分钟     |

**最短上线路径**：阶段 0→3 全做完，一个下午足够；内容是长期活，不用等站点完美才开始写。

**备份具体怎么做（别只依赖 GitHub）**：在 NAS 或任意电脑上执行一次，以后定期更新这个镜像即可：

```bash
git clone --mirror https://github.com/wxg007post/blog.git blog-mirror.git
git --git-dir=blog-mirror.git remote update        # 以后每次更新镜像
```

**说清楚每一层在防什么**（别误解成"只有 NAS 才安全"）：**GitHub 本身就是源码的云端副本**，替你保存着全部历史，所以"电脑坏掉"这件最日常的风险其实不用担心；NAS 镜像防的是**账号层面的意外**（账号被盗/被封、仓库被误删）。真正需要你**额外**操心的只有两样：① 将来放到 Cloudflare R2 的**图片桶**（不在 GitHub 里，丢了找不回）；② 仓库外的 `private/` 目录（含真实密钥，**只存在你本地**，建议单独加密备份）。

仓库里最核心的是 `content/`（文章正文）。**只要 `content/` 在，将来换框架、换托管平台都不慌。**

---

## 7. 安全与隐私红线（你的内容类型必须重视）

公开博客 = 全网可读，且**会被爬虫、快照、fork 永久留存**。你的折腾内容天然会碰到敏感信息，发布前必须自查：

| 必须脱敏的东西                                    | 处理方式                                 |
| ------------------------------------------ | ------------------------------------ |
| 内网 IP 段、公网 IP、MAC 地址                       | 一律写成 `192.168.x.x`、`10.0.0.x`、`公网IP` |
| Tailscale / ZeroTier 的 auth key、节点密钥、设备 ID | 写 `YOUR_AUTH_KEY`，后台截图要打码            |
| OpenWrt / 软路由 / NAS 的后台账号密码                | 只写"在 系统→管理权 里设置强密码"，绝不贴真实值           |
| Cloudflare API Token、域名商密钥、DDNS 密钥         | 用占位符，并在文章里提醒读者用环境变量                  |
| Clash/OpenClash 订阅链接（含 token）              | 打码或只贴格式示例；订阅转换后端地址同理                 |
| Docker Compose 里的密码/密钥                     | 用 `${VAR}` 引用，示例值写 `changeme`        |
| 家庭宽带账号、光猫超级密码、运营商信息                        | 不写                                   |
| 家人信息、住址、工作单位内部信息                           | 不写                                   |

**落地做法（落地时我会一起建好）**：

1. 仓库根目录放 `.gitignore`，并建一个本地 `private/` 目录专门放含真实密钥的配置文件，永不提交。
2. 文章写作统一走占位符规范（我会做一份 `脱敏检查清单.md`）。
3. ⚠️ **`draft: true` 不是隐私保护（这条我原来写错了，已修正）**：它只是让 Hugo **不构建**这篇，**文件本身仍然躺在公开仓库里，任何人都能翻到**。所以涉及密钥、内网真实细节的内容，**从一开始就不许写进仓库**——只写脱敏后的版本；确实要保留原文，就写在仓库**外面**的本地笔记里（或改用「私有源码 + 公开输出」结构，见 4.5，本次未采用）。
4. 万一误发：立刻改文件并推送（页面会更新），同时到 GitHub 搜索自己仓库的 fork，必要时改掉泄露的密钥——**密钥一旦泄露就必须轮换，删文章不算解决**。
5. **`.gitignore` 要覆盖的不只是 `public/`**：`public/`、`resources/_gen/`、`.hugo_build.lock`、`private/`、`secrets/`、`.env*`、`*.pem`、`*.key`、`*.p12`、`*.pfx`、`id_rsa`、`id_ed25519`、`Thumbs.db`、`.DS_Store`、`.obsidian/workspace*.json`。
6. **发布前自检脚本**（我会做一个 `发布前检查.ps1`，另配一个 3 行的 `发布前检查.cmd` 启动器方便双击；**真正的逻辑写在 PowerShell 里**——cmd 对中文路径和 UTF-8 容易乱码，启动器会先切到 UTF-8 代码页再调用它）。**这里有个设计要点：规则不能太笨，否则会造成"告警疲劳"**——第 7 章本身就教你把内网 IP 写成 `192.168.x.x` 占位符，如果脚本把 `192.168.` 一律当高危，那你的每篇教程都会报警，真正的风险反而被淹没。所以脚本分两档：

| 档位 | 检查什么 | 怎么处理 |
|---|---|---|
| **高危（红字，必须处理）** | 真实密钥格式（`sk-`、`ghp_`、`xoxb-`、`AKIA`）、私钥文件头（`BEGIN ... PRIVATE KEY`）、`.env` / `*.key` / `*.pem` / `id_rsa` 文件本身、`private/` 被提交、**超过 500KB 的大图** | 修掉再提交 |
| **提示（黄字，人工确认一下）** | **具体**的内网地址（如 `192.168.1.23`）、真实公网 IP、`password=` / `token=` 后面跟着真实值、可疑的长随机串 | 看一眼是不是教学示例 |

**明确豁免**（不报警）：`192.168.x.x`、`10.0.0.x`、`${VAR}`、`changeme`、`YOUR_...` 这类**占位符写法**；`.md` 教学文章里的字符串只按"提示"档处理，**只有配置文件才按"高危"档处理**。**它只是最后一道提醒，不能替代你自己的判断。**

**另外加一项"坏图检测"（编辑器无关的兜底）**：扫描每篇文章里的图片引用（`![...](路径)`），检查对应文件是否真实存在、是否用了绝对路径或 wiki 链接写法。因为不同编辑器写图片路径的方式不一样（MarkText 有"相对路径"相关的已知问题），这一步能在推送前就把**线上裂图**拦下来。

---

## 8. 成本清单

| 项目                   | 费用       | 备注                                    |
| -------------------- | -------- | ------------------------------------- |
| 域名 `wxgg.eu.cc`      | 已有       | 免费域名的续期政策请留意，别过期丢域名                   |
| GitHub Pages         | ¥0       | 免费账号：站点 ≤1GB、月流量 ≤100GB（软限制），个人博客远远够用 |
| GitHub Actions       | ¥0       | 公开仓库构建分钟数不限                           |
| Cloudflare DNS / CDN | ¥0       | 免费套餐（Web Analytics 本次不用）              |
| 评论（**本方案不做**）        | ¥0       | 按你的决定不上线；将来需要可自建 Twikoo / Waline      |
| 统计（**本次不做**）         | ¥0       | 将来需要时自建 Umami（Blowfish 原生支持）          |
| **合计**               | **¥0/年** | 唯一变数是域名：免费 `eu.cc` 续期**必须手动**（见 4.8）；可选花约 ¥70–90/年买个能长期持有的正式域名（如 `.com`）作主域                             |

---

## 9. 风险与常见坑

| 风险                              | 表现                     | 预防                                                              |
| ------------------------------- | ---------------------- | --------------------------------------------------------------- |
| GitHub 访问不稳定                    | 推送失败、Actions 排队久       | 挂代理推送；Git 配置 `http.proxy`；或改用 SSH over 443                      |
| 主题用 git submodule 但 Actions 没拉取 | 构建报错找不到主题              | Actions 里 `checkout` 加 `submodules: recursive`、`fetch-depth: 0` |
| Hugo 版本漂移                       | 换了 Hugo 版本后构建结果变化、样式错乱 | **本地与 CI 锁同一个版本号**（见 3.5）；升级前先在本地 `hugo server` 验证              |
| 站点地址与 `baseURL` 不一致             | 页面能打开但 CSS/图片/搜索全丢（白板） | 按 4.6 决定仓库类型；`baseURL` 结尾斜杠不能省；改完重新构建                           |
| Cloudflare 代理与 GitHub 证书冲突      | 重定向循环 / 525 / 526      | 严格按 4.1 顺序：先灰云签发，再橙云 + Full (strict)                            |
| ~~Hexo 依赖地狱~~（本方案不走 Hexo，此条已排除） | —（Hexo 不在本方案内）         | —                                                               |
| 主题升级打乱样式                        | 更新后版面错乱                | 更新前本地先验证；主题版本锁定并记录当前版本号                                         |
| 图片撑爆仓库                          | 仓库越来越大、推送变慢            | WebP 压缩；大量截图走 R2/NAS 图床                                         |
| 早期改 URL 规则                      | 已被搜索引擎收录的链接失效          | **一开始就定死永久链接格式**（如 `/posts/<英文slug>/`），后续改动必须加跳转                |
| 内容泄露隐私                          | 内网结构、密钥被公开             | 第 7 章清单，发布前逐项过一遍                                                |
| 免费服务政策变动                        | Pages 政策、域名续期、免费额度调整   | 内容是纯 Markdown，随时可迁走；每半年关注一次官方公告                                 |
| GitHub Actions 动作版本 / Hugo 资产名变更  | CI 报「找不到包」或「动作不存在」      | 工作流里已加 `hugo version` 自证；备用资产名与"对照官方 starter workflow"写在第 14 章；落地时先手动跑一次 |
| 草稿内容被公开                         | 以为 `draft: true` 就安全，其实文件可见 | 敏感内容**不写进仓库**（第 7 章第 3 条，已修正）                                   |
| Cloudflare 缓存导致新文章搜不到            | 文章已上线但站内搜索查不到               | 按 4.7 给 `/index.json` 设短缓存；排查时先「清除缓存」+ Ctrl+F5                     |
| Obsidian Wiki 链接导致线上坏图            | 本地正常、线上图片裂开                     | 按 5.6 关闭 Wiki 链接、新链接格式改为相对路径                                      |

---

## 10. 决策已全部定稿

**已全部定稿**：写作方式（本地 Markdown 一键发布）、域名（`blog.wxgg.eu.cc`，旧服务已清理）、框架（Hugo，**版本锁定**见 3.5）、主题（Blowfish v3）、仓库策略（公开仓库）、Git 方案（GitHub Desktop）、GitHub 用户名（`wxg007post`）、站内搜索（**本次上线，需支持中文**）、评论（不做）、访问统计（本次不做）、图床（后续用 Cloudflare R2）、中文显示（`hasCJKLanguage`）、代码复制按钮（`enableCodeCopy`）、分类与标签体系（分类 2 个 + 标签初始 10 个，见 5.3）。

**仓库名与站点类型（已定）**：采用**项目站点**，仓库名 `blog`（理由见 4.6）——绑定域名前地址是 `https://wxg007post.github.io/blog/`，绑定后即 `https://blog.wxgg.eu.cc/`。

**至此所有决策定稿，下一步只剩执行。**

---

## 11. 下一步（你确认后我可以直接做）

你说一声"按方案来"，我就在 `D:\DSH\my_blog` 里把这些东西全部建好并本地验证通过：

- 完整的站点骨架（Blowfish 主题 + `config/_default/` 全套配置 + 中文语言 + 菜单 + 关于页）
- **站内搜索已开好并实测通过**：按 3.6 覆盖 `assets/js/search.js`（中文分词）与 `layouts/_default/index.json`（把标签/分类也纳入索引），并用四类中文查询实测
- **Hugo 版本锁定**：本机与 GitHub Actions 使用同一个版本号（写入 3.5）
- **中文显示与代码复制按钮已配好**：`hasCJKLanguage = true`（hugo.toml）+ `enableCodeCopy = true`（params.toml），见 3.4.6
- `.github/workflows/deploy.yml` 自动部署流水线（推送到 GitHub 即自动上线）
- 三种文体的文章模板 + 2~3 篇示例文章
- `.gitignore` + `脱敏检查清单.md` + 一键发布脚本 `publish.cmd`
- 一份只讲"你要点哪里"的图文操作手册（面向非程序员，不需要看懂代码）
- `docs/versions.md` **版本记录**：Hugo 版本、主题 commit、Actions 动作版本、域名接入状态、搜索实现方式——以后样式/搜索/CI 出问题，一眼知道当时的环境
- `标签清单.md` **标签规范**：现有标签全表 + 大小写规范 + "新增前先查表、不新增语义重复标签"
- `发布前检查.ps1`（+ 双击用的 `发布前检查.cmd` 启动器）**发布前自检**：扫 `private/`、`.env`、`*.key`、`*.pem`、`id_rsa`、超过 500KB 的大图，以及 `192.168.`、`10.0.0.`、`password:`、`token:` 等高风险字符串（见第 7 章）
- 按第 15 章的**上线验收清单**逐项验证并交付结果
- 清理核查用的临时目录 `.research/`（我核对 Blowfish 源码时留下的原始依据，现保留 14 个小文件共 0.015 MB：主题的 `config.toml`、CI 工作流、模板仓库配置，以及中文搜索的实测脚本 `fusetest.cjs`；更大的主题快照已删除。你可随时打开自行核对，落地时我会把它删掉或写进 `.gitignore`，保证不进仓库）

需要 Git 安装、GitHub 仓库创建和授权，这些涉及你的账号，我会给你明确的点击步骤，由你来执行。

---

## 12. 这份方案你可以怎么改（编辑指引）

直接用记事本 / VS Code / Typora / Obsidian 打开 `博客建设方案.md` 就能改。**改完跟我说一声「改好了」**，我会重新完整读一遍，逐条复核，并把互相冲突、不现实或漏掉的地方指出来。约定如下：

| 你想做的事                         | 怎么写                                                   |
| ----------------------------- | ----------------------------------------------------- |
| 拍板一个决策                        | 在开头「决策记录」表里把 `⏳ 待定` 改成你的选择                            |
| 否掉或质疑某段方案                     | 不要直接删掉，在该行后面补 `<!-- 我的意见：... -->`，或另起一行写 `> ❓ 疑问：...` |
| 补充新需求（友链、相册、说说、书单、资源下载页、归档页等） | 写在下面「我的补充需求」里，格式随意，我负责落实                              |
| 暂时拿不准的                        | 标上 `【待定】`，复核时我给你建议和取舍理由                               |
| 保存                            | UTF-8 编码（记事本默认即是），文件名不要改                              |

改完之后可能的三种结果，我都会明确告诉你：**① 直接采纳；② 采纳但需要改别处（我会列出连带影响）；③ 不建议，理由 + 替代方案。**

## 13. 我的补充需求（你随便写，我负责落实）

- 要注意多中文的支持，包括站内搜索对中文的支持
  - ✅ 已收到，分两层落实：**界面中文**（`languages.zh-cn.toml` + 语言代码 `zh-cn`，见 3.4.3）；**中文搜索**（Blowfish 用 Fuse.js，中文没有词间空格，默认配置可能搜不全——核实结论与调优方案见 **3.6**，落地时我会直接配置好并用中文关键词实测）。

---

## 14. 附：GitHub Actions 部署工作流（照此落地）

> 这份 YAML 的动作版本与 Hugo 版本取值，均照抄/对齐 Blowfish 主仓库自己的 CI（`pages.yml`），不是凭印象写的。落地时我会直接用它，并把仓库名等参数填好。

```yaml
name: Deploy Hugo site to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:          # 支持在网页上手动点一次重新部署

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

env:
  HUGO_VERSION: 0.165.0       # 必须落在主题声明的 0.162.0–0.165.0 区间内（见 3.5）

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Hugo extended CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb \
            https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
          hugo version   # 装完立刻自证：应为 v0.165.0+extended；这步失败说明包名或版本不对

      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive   # 不写这行：themes/blowfish 会是空目录，构建直接失败
          fetch-depth: 0          # 不写这行：文章的「最后更新」时间取不到真实提交时间

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Build with Hugo
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: hugo --gc --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

**四个关键参数为什么这样写**：

| 参数                                                 | 作用                      | 不写的后果                       |
| -------------------------------------------------- | ----------------------- | --------------------------- |
| `HUGO_VERSION: 0.165.0`                            | 本地与云端用同一个版本             | 超出主题区间 → 构建报错（见 3.5）        |
| `submodules: recursive`                            | 把主题（git submodule）一起拉下来 | `themes/blowfish` 为空 → 构建失败 |
| `fetch-depth: 0`                                   | 拉取完整提交历史                | 文章最后更新时间不准                  |
| `--baseURL "${{ steps.pages.outputs.base_url }}/"` | 让 GitHub 自动给出站点地址       | 项目站点会因地址带子路径而样式全丢           |

**关于这四个动作版本号（实测结论，补于 v1.9）**：我在 2026-09-12 逐个验证过 `actions/checkout@v7`、`actions/configure-pages@v6`、`actions/upload-pages-artifact@v5`、`actions/deploy-pages@v5`——**这四个 ref 都真实存在**（旧版 v4 / v5 / v3 / v4 也都还在）。所以版本号本身不是问题。
**但**：GitHub 会弃用旧版动作，主题仓库用的也可能是"超前"版本，所以**"最终用哪个版本"以落地时实际跑通为准**——先 `workflow_dispatch` 手动跑一次，绿了再定版，并把实际使用的版本号记进 `docs/versions.md`。

**两个"万一失败"的备用方案（提前写下来，出事时不用查）**：

1. **`.deb` 文件名已实测确认（2026-09-12）**：0.165.0 的 deb 资产名是**小写**的 `hugo_extended_0.165.0_linux-amd64.deb`。我逐个验证过：`..._Linux-64bit.deb` 返回 **404（不存在）**，`..._linux-amd64.deb` 存在。上面工作流里的名字已改成**能用的那个**。
   - ⚠️ **这里有个值得记住的教训**：**Blowfish 主仓库自己的 `pages.yml` 里写的正是会 404 的那个名字**（它很可能已不再使用该工作流、改为 Netlify 部署）。所以「别人的 CI 里这么写」**不能**当作"一定可用"的证据——**唯一可靠的验证是下载一次**。我最初就是被这条误导的。
   - 备用方案：`.deb` 方式若在你的环境失败，`.tar.gz` 的两种命名都存在（`Linux-64bit.tar.gz` 与 `linux-amd64.tar.gz` 都有效），下载解压后把 `hugo` 放进 `PATH` 即可。
   - 工作流里那行 `hugo version` 就是用来**第一时间暴露**包名或版本问题的。
2. **某个 `uses:` 动作版本不可用**（GitHub 会弃用旧版动作）→ 对照 GitHub 官方的 Pages starter workflow，把版本号换成当前可用版本。本工作流里的 `checkout@v7` / `configure-pages@v6` / `upload-pages-artifact@v5` / `deploy-pages@v5` 同样**取自 Blowfish 主仓库当时的 CI**，因此当时有效；但**落地时我会先在仓库里手动跑一次（`workflow_dispatch`）确认全绿，再往下走**。

**关于地址的一个细节**：`base_url` 是 GitHub 根据仓库设置算出来的——你现在是项目站点（仓库 `blog`），它会给出 `https://wxg007post.github.io/blog/`；**等你在 Pages 设置里填了 `blog.wxgg.eu.cc`，它会自动变成 `https://blog.wxgg.eu.cc/`**。所以这份工作流你不需要手工改地址，绑域名前后都是对的。

**部署前还需要做两次设置**：

1. 仓库 `Settings → Pages → Build and deployment → Source` 选 **GitHub Actions**（不是 "Deploy from a branch"）。
2. **确认仓库的默认分支名是 `main`**：工作流里写的是 `branches: ["main"]`。如果你建仓库时默认分支叫 `master`，推送后**根本不会触发部署**——现象是"代码推上去了但网页永远不更新"，很容易误判成坏了。落地时我会先核对分支名；不一致就把工作流里的 `main` 改成实际分支名（或把仓库默认分支改名）。

---

## 15. 上线验收清单（照着一项项打勾）

> 这份清单是「什么叫可以了」的最终定义。落地后我会带着你逐项过一遍，**不通过就不算上线完成**。

### 15.1 本地检查

- [ ] `hugo server -D` 能起服务，首页 / 文章页 / 分类页 / 标签页 / 关于页都能打开
- [ ] 中文文章**不显示**「1 词 · 1 分钟阅读」（说明 `hasCJKLanguage` 生效了）
- [ ] 阅读时间看起来合理（一篇长文不该只写 1 分钟）
- [ ] 代码块的**复制按钮**桌面端可用、手机端可用
- [ ] 深色模式切换正常
- [ ] 文章头图、正文图片都显示正常（不是裂图）
- [ ] 分类**只有两个**：折腾记录、攻略
- [ ] 标签里没有大小写不一致的（`docker` / `Docker` 这种）

### 15.2 搜索检查（四类中文查询，全过才算）

- [ ] 连续词命中（如 `搜索`）
- [ ] **带空格的多词**命中（如 `中文 搜索`）
- [ ] **标签名**命中（如 `飞牛`）
- [ ] **分类名**命中（如 `攻略`）
- [ ] 单个汉字不产生满屏无意义结果
- [ ] 新文章发布后，搜索索引能在合理时间（几分钟）内更新

### 15.3 线上检查

- [ ] `https://blog.wxgg.eu.cc` 能打开
- [ ] 输入 `http://` 会自动跳转到 `https://`
- [ ] GitHub Pages 设置里的 **Enforce HTTPS** 已勾选（不能只靠 Cloudflare 的 Always Use HTTPS）
- [ ] 浏览器地址栏小锁头正常（证书有效）
- [ ] **样式不白板**（说明 `baseURL` 正确）
- [ ] 页面源码里 CSS/JS 地址与 `canonical` 都指向 `https://blog.wxgg.eu.cc/`
- [ ] `/index.json`、`/sitemap.xml`、`/rss.xml` 都能打开
- [ ] 手机上打开正常（排版不乱、不横向滚动）
- [ ] 推送后 Actions 是绿勾；Cloudflare 侧 SSL 为 Full (strict)，无重定向循环

### 15.4 安全检查（最重要）

- [ ] 仓库里**没有**真实密码、API Token、订阅链接、内网真实 IP
- [ ] 截图里没有露出后台界面、密钥、设备 ID
- [ ] `private/` 目录没有被提交
- [ ] `.env` / `*.key` / `*.pem` / `id_rsa` 没有被提交
- [ ] **草稿内容也检查过**（草稿只是不构建，文件在公开仓库里是可见的，见第 7 章）

### 15.5 第一版刻意不做的事（避免一开始就复杂）

| 不做 | 原因 |
|---|---|
| 评论、访问统计 | 你的决定（见决策表 10、11） |
| 图床 / R2 | 第二阶段再说（决策表 12） |
| 中文 WebFont | 中文字体动辄几 MB，**优先用系统字体**；真要自定义只加载拉丁字符子集 |
| 首页大图 / 重动效布局 | 第一版用 `profile` 或 `card` 这类轻布局（`page` 也行），先把内容跑通 |
| `summaryLength` 大改 | 先用默认 70，觉得短再调 |
| Pagefind / 自定义搜索 | 按 3.6.4 的回退触发条件再启用 |
