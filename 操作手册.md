# 操作手册（阶段 2：已发布到 GitHub，还差启用 Pages）

> 这份手册只讲"你要点哪里 / 要做什么决定"。技术细节在《博客建设方案.md》。
> 更新时间：2026-09-23｜当前阶段：**阶段 2 进行中 —— 代码已推上 GitHub，只差"启用 Pages"这一步**

---

## 一、现在有什么

| 项目 | 状态 |
|---|---|
| Hugo 0.165.0 extended | ✅ 已装（`%LOCALAPPDATA%\Programs\hugo`，已在 PATH） |
| Blowfish 主题 | ✅ **git submodule** 引入，**锁定 commit `4643c46`** |
| 中文界面与计数 | ✅ `zh-cn` + `Asia/Shanghai` + `hasCJKLanguage`（不会出现「1 词 · 1 分钟阅读」） |
| 代码复制按钮 | ✅ 已开启并核实生效 |
| 站内搜索 | ✅ 已开启，并修掉主题两个缺陷（空列表页污染索引、索引不含标签/分类） |
| **第一篇文章** | ✅ 已写好：`content/posts/hugo-blowfish-github-pages-blog/`（2452 字，`draft: false`，**推送后即上线**） |
| 示例文章 | 2 篇仍为草稿（`draft: true`），线上不会出现；想删随时说 |
| GitHub 仓库 | ✅ `https://github.com/wxg007post/blog`（公开，已验证可访问） |
| GitHub Actions | ✅ 工作流已推送；**首次运行失败**，原因见第四节 |
| 工程文件 | ✅ `.gitignore`、`发布前检查.cmd`、`docs/versions.md`、`标签清单.md` |

---

## 二、本地预览

预览服务正在运行（独立进程，不依赖我的会话）：

### 👉 http://localhost:1313/

可以看到：首页（头像卡片 + 最近文章）、**第一篇文章**、分类页、标签页、「关于」页、深色模式、搜索（按 `Ctrl+K`）。

> 自己启动：在 `D:\DSH\my_blog` 打开终端敲 `hugo server -D`，然后开 http://localhost:1313/ ；关掉终端即停止。
> 若页面显示 "Hugo Server: Error"，说明上次重建失败卡住了——告诉我，我重启。

---

## 三、站点信息（✅ 已设置）

| 位置 | 现在的值 |
|---|---|
| 站点名称 | **时光笔记** |
| 作者显示名 | **WXG** |
| 站点副标题 | 随便记点什么，只是做个记录。 |
| 头像 | `assets/img/avatar.jpg` —— **必须在 `assets/`，放 `static/` 会导致整个站点构建失败** |
| 首页布局 | `profile` 卡片式（头像 + 名字 + 简介 + 最近文章） |

想改任何一项，告诉我即可（配置在 `config/_default/languages.zh-cn.toml`）。

---

## 四、阶段 2：还差 1 步

| 步骤 | 状态 |
|---|---|
| 1. 本地仓库加进 GitHub Desktop | ✅ |
| 2. 发布到 GitHub（公开） | ✅ 已确认 `github.com/wxg007post/blog` 返回 200 |
| 3. **启用 Pages** | ⏳ **就差这一步** |

### 为什么必须启用

工作流首次运行**失败**，报错原文：

```
Setup Pages
HttpError: Not Found - https://docs.github.com/rest/pages/pages#get-a-apiname-pages-site
```

含义：**仓库还没启用 Pages**。Actions 想替你部署，但 Pages 没打开，它查 Pages 站点时拿到 404。

### 你要点的两下（约 30 秒）

1. 打开 **https://github.com/wxg007post/blog/settings/pages**
2. 把 **Build and deployment → Source** 从 "Deploy from a branch" 改成 **GitHub Actions**（自动保存）

### 然后推送第一篇文章

在 GitHub Desktop 里会看到待推送的提交 → 点 **Push origin**。推送会**自动触发**工作流，不用手动 Re-run。

> 想先看文章再推？在 Obsidian 里打开 `content/posts/hugo-blowfish-github-pages-blog/index.md` 直接改，改完再推。
> 也可以只启用 Pages，然后去 **Actions → 失败的那次运行 → Re-run all jobs** 立刻重跑。

---

## 五、常见问题

| 现象 | 解决 |
|---|---|
| 线上站点 404 / "Site not found" | 十有八九是 Pages 没启用，或工作流还没跑完（首次约 1~2 分钟） |
| 工作流报 `HttpError: Not Found` | 同上：去 Settings → Pages 把 Source 改成 GitHub Actions |
| 本地预览打不开 1313 | 端口被占或服务已停；告诉我，我重启 |
| 预览显示 "Hugo Server: Error" | 上次重建失败卡住了；改一下内容或让我重启 |
| 怎么新建文章 | Obsidian：模板文件夹设为 `content/_templates`，用 `_文章模板.md`；命令行：`hugo new posts/文章名/index.md` |
| 新文章首页看不到 | 看 front matter 的 `draft:` —— `true` 是草稿（本地 `hugo server -D` 可见、线上不发布） |
| 图片线上不显示 | 图片要与 `index.md` **同一文件夹**、引用写相对路径；推送前跑一次 `发布前检查.cmd` |
| **头像 / logo 放哪里** | **必须 `assets/img/`**，不能 `static/`（主题用 `resources.Get` 取图，放错会让构建失败） |
| `winget` 报错 | 本机 winget 不可用；Hugo 已用"手动解压 + 校验 SHA256"装好 |
| 想改站点名 / 菜单 / 首页布局 | 告诉我，或改 `config/_default/` 下对应文件 |
