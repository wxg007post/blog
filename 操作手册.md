# 操作手册（阶段 1：本地站点已搭好）

> 这份手册只讲"你要点哪里 / 要定什么"。技术细节在《博客建设方案.md》。
> 更新时间：2026-09-12｜当前阶段：**阶段 1 已完成 ✅，等待你的指令进入阶段 2**

---

## 一、现在有了什么

| 项目 | 状态 |
|---|---|
| Hugo 0.165.0 extended | ✅ 已装（`%LOCALAPPDATA%\Programs\hugo`，已在 PATH） |
| Blowfish 主题 | ✅ 以 **git submodule** 引入，**锁定 commit `4643c46`**（2026-09-03） |
| 中文界面 | ✅ 语言 `zh-cn`、时区 `Asia/Shanghai`、`hasCJKLanguage` 已开 |
| 中文计数 | ✅ 示例文章显示「530 字 · 2 分钟」（**不再出现「1 词 · 1 分钟阅读」**） |
| 代码复制按钮 | ✅ 已开启（`enableCodeCopy`） |
| 站内搜索 | ✅ 已开启，并修掉了主题的两个缺陷（见下） |
| 文章模板 | ✅ 命令行用 `archetypes/default.md`；Obsidian 用 `content/_templates/_文章模板.md` |
| 示例文章 | ✅ 2 篇（**草稿状态**，发布前把 `draft` 改成 `false` 即可） |
| `.gitignore` | ✅ 已写好（Hugo 产物、密钥类文件、`.research/` 都不会进仓库） |
| 发布前检查 | ✅ `发布前检查.cmd`（双击即可运行，含**坏图检测**） |
| 版本记录 / 标签清单 | ✅ `docs/versions.md`、`标签清单.md` |

### 顺手修掉的两个主题缺陷（这就是"搜不到文章"的真实原因）

1. **搜索索引里混进了 `Tags`、`Categories`、`Posts` 这些空列表页** —— 主题默认遍历 `.Site.Pages`（全部页面），搜索结果里会出现这些没有内容的条目。已改成**只收录 `content/posts/` 下的真实文章**。
2. **索引里根本没有 tags / categories 字段** —— 所以**搜标签名、分类名永远搜不到**。已把这两个字段加进索引，并在搜索配置里加上对应权重。

实测对比（用主题自带的 Fuse 引擎，改动前 → 改动后）：

| 查询 | 改动前 | 改动后 |
|---|---|---|
| `转码`（连续词） | 2 条 | 2 条 |
| `群晖 Docker`（带空格多词） | 1 条 | 2 条 |
| `选型`（**只存在于标签里**） | **0 条** | **1 条** ✅ |
| `折腾记录`（**只存在于分类里**） | **0 条** | **1 条** ✅ |

> 目前索引里只有 2 篇示例文章，样本还小；等你写到 10 篇左右我会再复测一次。
> 结论：**方案 3.6 里"分两步走"的设计奏效了——只做了第一步（最小改动）就达标，不需要上自定义中文分词函数**，以后主题升级要 diff 的文件也最少。

---

## 二、你现在可以做的：本地预览

我已经在后台把预览服务跑起来了，浏览器打开：

### 👉 http://localhost:1313/

能看到：首页（站点简介 + 最近文章）、两篇示例文章、分类页、标签页、「关于」页。右上角可切深色模式；点搜索图标或按 `Ctrl+K` 可以试中文搜索（试试搜 `选型`、`折腾记录`、`群晖 Docker`）。

> 自己启动的方法：在 `D:\DSH\my_blog` 打开终端，敲 `hugo server -D`（`-D` = 连草稿一起显示）。

---

## 三、站点信息（✅ 已按你的要求设置）

| 位置 | 现在的值 |
|---|---|
| 站点名称 | **时光笔记** |
| 作者显示名 | **WXG** |
| 站点副标题 | 随便记点什么，只是做个记录。 |
| 头像 | `static/img/avatar.jpg`（你给的叶子图，320×320，12.6 KB） |
| 首页布局 | **`profile`** 卡片式：头像 + 名字 + 一句话简介 + 最近文章 |

想改任何一项，告诉我即可（都在 `config/_default/languages.zh-cn.toml`）。
若想换回最简的纯文字首页，把 `config/_default/params.toml` 里 `[homepage]` 的 `layout` 从 `profile` 改回 `page`。

---

## 四、阶段 2 要做什么（**需要你点几下鼠标**）

阶段 2 = 把本地仓库推到 GitHub，并让 GitHub Pages 自动构建。核心就 3 步：

1. 打开 **GitHub Desktop** → 菜单 `File → Add local repository` → 选择 `D:\DSH\my_blog`；
2. 点右上角 **Publish repository** → 仓库名填 **`blog`** → **取消勾选 "Keep this code private"**（必须公开：免费账号的 Pages 只支持公开仓库）；
3. 回来告诉我"发布好了"，我接着验证流水线并配置 Pages 设置。

> 等你说「开始阶段 2」，我会把这三步再列一遍（含每一步会看到什么），并在你发布后立刻检查构建结果。

---

## 五、常见问题

| 现象 | 解决 |
|---|---|
| 本地预览打不开 1313 | 端口可能被占用；告诉我，我换端口重启 |
| 怎么新建文章 | Obsidian：模板文件夹设为 `content/_templates`，用 `_文章模板.md`；命令行：`hugo new posts/文章名/index.md` |
| 新文章首页看不到 | 看 front matter 的 `draft:` —— `true` 是草稿（本地 `hugo server -D` 能看到，线上不发布） |
| 图片线上不显示 | 图片要和 `index.md` **同一文件夹**，引用写相对路径；推送前跑一次 `发布前检查.cmd` |
| `winget` 报错 | 本机 winget 不可用；Hugo 已用"手动解压 + 校验 SHA256"装好 |
| 想改站点名 / 菜单 / 首页布局 | 告诉我，或改 `config/_default/` 下对应文件 |
