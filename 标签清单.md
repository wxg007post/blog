# 标签清单（写文章时照这个来）

> 规则详见《博客建设方案.md》5.3：
> 1. 一篇文章**最多 5 个标签**；
> 2. 泛指与专指**可以并存**（`NAS` + `飞牛`），但别堆语义重复的（不要 `OpenWrt` + `OpenWrt 固件`）；
> 3. **大小写必须统一** —— Hugo 把 `docker` 和 `Docker` 当成两个不同标签，标签页会被拆散；
> 4. 想加新标签，先看下面有没有近义的；加了就顺手补到这张表里。

## 现有标签（12 个，2026-09-23 更新）

| 标签 | 说明 |
|---|---|
| `NAS` | 泛指：NAS 相关（群晖 / 威联通 / 飞牛 / 自组都算） |
| `飞牛` | 飞牛 fnOS 系统 |
| `Docker` | 容器、docker compose、镜像 |
| `OpenWrt` | OpenWrt 系统与固件 |
| `ImmortalWrt` | ImmortalWrt 分支 |
| `OpenClash` | 代理与分流相关 |
| `Tailscale` | 内网穿透、子网路由 |
| `软路由` | 硬件角色（软路由设备本身） |
| `AI` | AI 工具、模型、用法 |
| `工具` | 其他小工具与脚本 |
| `Hugo` | Hugo 站点生成器相关（2026-09-23 新增） |
| `GitHub Pages` | GitHub Pages / Actions / 自动部署（2026-09-23 新增） |

## 候选标签（用到再加，新增是零成本）

`选型`、`避坑`、`群晖`、`威联通`、`硬件`、`游戏`、具体游戏名、`IPv6`、`端口映射`、`硬件转码`、`光猫桥接`、`Jellyfin`……

## 维护

每季度过一遍：把"只出现过一次、且意义不大"的标签合并或删除；**游戏名这类有意义的单次标签保留**。
